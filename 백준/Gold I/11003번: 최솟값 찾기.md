[https://www.acmicpc.net/problem/11003](https://www.acmicpc.net/problem/11003)
```javascript
const input = require('fs')
    .readFileSync(process.platform === 'linux' ? '/dev/stdin' : './input.txt')
    .toString()
    .trim()
    .split('\n');

let inputIndex = 0;

//N: 개수, L
const [N, L] = input[inputIndex++].split(' ').map(Number);

// 숫자 배열
const nums = input[inputIndex++].split(' ').map(Number);

// 최솟값 담아놓는 배열
const answer = [];

// 모노톤 덱. 여기에 인덱스를 저장. 인덱스가 가리키는 값 기준 오름차순으로 정렬되어 있다.
const d = [];
let head = 0;

// 일단 N번 순회
for (let i = 0; i < N; i++) {
    // 덱의 크기가 0보다 커야하고, 덱의 첫 번째 요소(인덱스)가 유효한 (i-L+1) 인덱스이어야 한다.
    if (d.length > head && d[head] < i - L + 1) {
        head++; // 유효하지 않다면 덱에서 제거
    }

    // 현재 넣을 값 nums[i] 보다 큰 숫자는 전부 제거한다. 덱은 오름차순 인덱스 순으로 정렬된다.
    while (d.length > head && nums[d[d.length - 1]] > nums[i]) {
        d.pop();
    }

    // 인덱스 덱에 넣기. 앞에서 i번째 숫자보다 큰 수는 전부 제거했으니, 덱은 오름차순을 유지한다.
    d.push(i);

    // answer에 최솟값 기록
    answer.push(nums[d[head]]);
}

console.log(answer.join(' '));
```
### 🔗 풀이
1. 🤔 시뮬레이션 돌려보기
    - i=1 : A-1 ~ A1 -> 1이 최소
    - i=2 : A0 ~ A2 -> 1이 최소
    - i=3 : A1 ~ A3 -> 1이 최소 
    - i=4 : A2 ~ A4 -> 2가 최소

2. 🤔 A범위를 넘어간 애들은 무시해야 한다.
    - `L`이 윈도우 크기인 셈이다.

3. 🤔 덱에 인덱스 넣기
    - 값을 넣으면 안된다.
    - 덱 !== 윈도우. 덱에는 윈도우 크기만큼 값이 들어가 있지 않는다. 여기는 인덱스를 넣어야 한다.
### 🔗 배운점
1. 덱에는 "인덱스"가 들어간다. 값 자체가 들어가는 것이 아니다.
   - 덱 !== 윈도우를 기억하자. 슬라이딩 윈도우가 이동하면 오래된 값은 버려져야 한다. 인덱스가 없다면 그 값이 언제 들어온건지 정보가 없다면 버려야 하는지 파악할 수 없다.
   - 즉, 위치 정보를 덱에 넣는다고 보면 된다.
2. 덱의 길이 조건(`length > head` 또는 `length > 0`)이 꼭 있어야 하는 이유
   - 덱이 비어있는데 조회하면 `undefined`를 반환한다.
3. ⭐️ 모노톤 덱의 순서: 자연어 한 줄 요약
   1. 윈도우 범위를 벗어난 앞쪽 인덱스 제거
   2. 새로 들어온 인덱스의 값보다 가망 없는 뒤쪽 인덱스 제거
   3. 현재 인덱스 등록
   4. 덱 맨 앞의 값 정답에 기록
4. 모노톤 덱 스니펫
   ```javascript
      function monotonicDequeTemplate(nums) {
          const answer = [];
          const deque = []; // 오직 '인덱스'만 저장
          let head = 0;     // shift() 최적화 포인터
      
          for (let i = 0; i < nums.length; i++) {
              
              // 1단계: [앞쪽 청소] 유통기한 확인
              // "맨 앞의 1등이 이제 죽을 때가 되었는가?"
              while (deque.length > head && /* 💡 [조건 1] 앞쪽 제거 조건 */) {
                  head++;
              }
      
              // 2단계: [뒷쪽 청소] 단조(Monotonic) 성질 유지
              // "새로 들어온 나보다 약한(쓸모없는) 녀석들이 있는가?"
              while (deque.length > head && /* 💡 [조건 2] 뒷쪽 제거 조건 */) {
                  deque.pop();
              }
      
              // 3단계: [등록]
              // "나의 위치(인덱스)를 덱의 맨 뒤에 등록한다."
              deque.push(i);
      
              // 4단계: [정답 기록]
              // "조건이 만족되었으니, 현재 1등(맨 앞)을 정답에 적는다."
              if (/* 💡 [조건 3] 정답 기록 조건 */) {
                  answer.push(nums[deque[head]]);
              }
          }
      
          return answer;
      }
   ```
