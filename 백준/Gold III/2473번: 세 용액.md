[https://www.acmicpc.net/problem/2473](https://www.acmicpc.net/problem/2473)
```javascript
const input = require('fs')
    .readFileSync(process.platform === 'linux' ? '/dev/stdin' : './input.txt')
    .toString()
    .trim()
    .split('\n');

let inputIndex = 0;

// 전체 용액의 수(3~5000)
const N = Number(input[inputIndex++]);

const solvents = input[inputIndex]
    .split(' ')
    .map(Number)
    .sort((a, b) => a - b);

let answer;

// 최솟값
let minSum = Number.MAX_SAFE_INTEGER;

// 무조건 섞을 용액 하나를 집는다
for (let i = 0; i < N; i++) {
    // 무조건 넣을 용액
    const required = solvents[i];

    // 왼쪽 포인터: 기준점 바로 다음
    let left = i + 1;

    // 오른쪽 포인터: 배열 마지막
    let right = N - 1;

    // while문의 역할은, 전체 경우의 수를 확인하면서 3용액의 합을 최소화하는 것이다.
    while (left < right) {
        // 세 용액을 합친다
        const sum = required + solvents[left] + solvents[right];

        // 최솟값이라면 저장한다.
        if (minSum > Math.abs(sum)) {
            // 절댓값 = 거리
            minSum = Math.abs(sum);
            answer = [required, solvents[left], solvents[right]];
        }

        // 만약 결과가 0이라면 더 이상 포인터를 움직일 필요가 없다.
        if (sum === 0) {
            break;
        }
        // 결과가 양수일 때 -> 합을 줄인다. right--
        if (sum > 0) {
            right--;
        } else if (sum < 0) {
            // 결과가 음수일 때 -> 합을 늘린다. left++
            left++;
        }
    }
}

console.log(answer.join(' '));
```
### 🔗 풀이
1. 💡 조건 정리
    - 산성, 알칼리 각각 1~10억 -> 숫자 무척 크다.
    - 목표 -> 세 가지 용액을 혼합하여 특성값이 0에 가장 가까운 용액을 만들기

2. 🤔 조합 + 백트래킹 문제인가? -> ❌
    - 정렬된 배열에서 3개의 숫자를 더해 0과 가까운 값을 계속 선택한다.
    - 배열의 길이는 최대 5000이다. 약 208억번의 연산이 발생하여 시간 초과가 난다.

3. 🤔 이분 탐색?
    - 일단 정렬한다.
    - 우리가 궁금한 것은 "0과 가까운가?"" 이다. 어떻게 계산해야 쉽게 판단할 수 있을까?
    - 케이스가 나뉠 것 같다.
        1. 산성/알칼리만 있는 경우 -> 산성의 경우 앞에서 3개, 알칼리의 경우 뒤에서 3개의 합이 0과 가장 가깝다.
        2. 섞인 경우 -> 비슷한 특성값으로 상쇄해야 한다.
    - 🤔 일일이 비교해야 할까?
        - 너무 경우의 수가 많다...

4. 🤔 투 포인터? -> ✅
    - 이중 for문은 가능하다.
    - 용액 한 개를 결정한다.
    - 이후 2개를 포인터 이동하면서 구한다.
### 🔗 배운점
1. 내부 반복문 시작점이 `i+1`인 이유
    - 조합이기 때문이다. 기준점 이전 값은 이미 확인했다.
