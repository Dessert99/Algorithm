[https://www.acmicpc.net/problem/1946](https://www.acmicpc.net/problem/1946)
```javascript
const input = require('fs')
    .readFileSync(process.platform === 'linux' ? '/dev/stdin' : './input.txt')
    .toString()
    .trim()
    .split('\n');

let inputIndex = 0;

const answer = [];

// 테스트 케이스의 개수 1~20
const T = Number(input[inputIndex++]);

// 각 테스트 케이스 실행
for (let t = 0; t < T; t++) {
    // 지원자 수 1~10만
    const N = input[inputIndex++];

    // 지원자 순위
    const rankings = [];
    for (let i = 0; i < N; i++) {
        // [서류 심사 순위, 면접 심사 순위]
        rankings.push(input[inputIndex++].split(' ').map(Number));
    }

    // 정렬한다. 서류 순위 오름차
    // 참고로 동석차 없이 결정된다
    const sortedRankings = rankings.sort((a, b) => a[0] - b[0]);

    // 최솟값
    let minGrade = N + 100;

    let count = 0;

    // 이떄, 바로 앞사람만 확인하는 것이 아니라, 최솟값을 기억해놔라.
    for (let i = 0; i < N; i++) {
        const curGrade = sortedRankings[i][1];

        // 최솟값과 비교해서 순위가 더 높다면 (값이 작다면)
        if (curGrade < minGrade) {
            // 합격
            count++;
            // 최솟값 갱신
            minGrade = curGrade;
        }
    }

    // answer 배열에 최대 인원수 넣기
    answer.push(count);
}

console.log(answer.join('\n'));

/*
💡 지원자가 "탈락"하는 경우
    - 본인보다 서류, 면접 모든 부분에서 순위가 높은 사람이 한 명이라도 있다면 탈락이다.
    - 예를 들어, (1,1) 순위인 참가자가 있다면, 다른 모든 사람은 탈락이다.
    - 하지만 (1, 4) 순위인 참가자가 있다면, (2,2) 인 사람은 서류에서는 순위가 낮지만 면접 순위가 높기 떄문에 생존이다.

🤔 정렬하면 어떻게 될까?
기준1. 서류 심사 순위 오름차순
*/
```
### 🔗 풀이
1. 서류 순위 기준 오름차순 정렬한다. 이제 면접 순위만 보면 된다.
2. 면접 순위가 가장 낮은(값이 높은) 사람을 기억한다.
3. 최솟값(면접 순위)을 기준으로 더 작은 (순위가 낮은)사람은 탈락시키고, 최솟값을 갱신한다.


### 🔗 배운점
1. 다중 정렬 방법 (`sort()`)
    
    ```jsx
    data.sort((a, b) => 첫번째 조건 || 두번째 조건 || 세번째 조건);
    ```
    
2. 근데 이 문제에서 다중 정렬이 의미 없는 이유
    - 동석차는 없기 때문에 서류 순위에서 동점자는 없다.
3. 단순히 앞사람하고만 비교하면 안되는 이유
    
    ```jsx
    1 10
    2 15 -> 탈락
    3 6  -> 통과
    4 8  -> 통과 : 바로 앞사람보다 모든 순위가 낮지만, 1 10 보다 면접 순위가 높다.
    ```
    
    - 해결 방법
        
        > 최솟값을 기억해놓자.
        > 
        
        ```jsx
          // 최솟값
          let minGrade = N + 100;
        
          let count = 0;
        
          // 이떄, 바로 앞사람만 확인하는 것이 아니라, 최솟값을 기억해놔라.
          for (let i = 0; i < N; i++) {
              const curGrade = sortedRankings[i][1];
        
              // 최솟값과 비교해서 순위가 더 높다면 (값이 작다면)
              if (curGrade < minGrade) {
                  // 합격
                  count++;
                  // 최솟값 갱신
                  minGrade = curGrade;
              }
          }
        
        ```
