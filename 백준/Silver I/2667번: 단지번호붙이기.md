[https://www.acmicpc.net/problem/2667](https://www.acmicpc.net/problem/2667)

```javascript
const input = require('fs')
    .readFileSync(process.platform === 'linux' ? '/dev/stdin' : './input.txt')
    .toString()
    .trim()
    .split('\n');

let index = 0;

// 지도 크기: 5≤N≤25
const N = Number(input[index++]);

// 단지내 집의 수 배열 (오름차순 정렬 필요)
const houseComplexArr = [];

// 집 현황 (0과 1로 채워짐)
const maps = [];
for (let i = 0; i < N; i++) {
    maps.push(input[index++].split('').map(Number));
}

// 방문 여부를 나타내는 visited
const visited = Array.from({ length: N }, () => Array(N).fill(false));

// BFS에서 쓰일 dx,dy
const dx = [-1, 0, 1, 0];
const dy = [0, 1, 0, -1];

// maps 탐색
for (let y = 0; y < N; y++) {
    for (let x = 0; x < N; x++) {
        // 단지 탐색을 시작하는 조건: 1)유효한 maps 2)방문 안한 곳
        if (maps[y][x] === 1 && visited[y][x] === false) {
            // BFS 당 하나씩 쓰일 큐
            const queue = [];

            // 하나의 단지에 있는 집을 셀 변수
            let houseCount = 0;

            // 방문 표시
            visited[y][x] = true;

            //방문 예약
            queue.push([y, x]);

            // BFS 탐색
            while (queue.length > 0) {
                const [curY, curX] = queue.shift();
                //여기에서 각 단지의 집 개수를 카운팅한다.
                houseCount++;

                // 상하좌우 탐색
                for (let n = 0; n < 4; n++) {
                    const ny = curY + dy[n];
                    const nx = curX + dx[n];

                    // 탐색 후보를 결정하는 조건
                    if (ny >= 0 && nx >= 0 && ny < N && nx < N && visited[ny][nx] === false && maps[ny][nx] === 1) {
                        //방문 표시
                        visited[ny][nx] = true;

                        //방문 예약
                        queue.push([ny, nx]);
                    }
                }
            }
            // BFS가 끝난 상태 -> 단지 한 개를 돌며 집을 셌다.
            houseComplexArr.push(houseCount); // 배열에 넣기
        }
    }
}

// 단지내 집의 수 배열 오름차순 정렬
houseComplexArr.sort((a, b) => a - b);

console.log(houseComplexArr.length);
console.log(houseComplexArr.join('\n'));
```

### 🔗 배운점
1. 처음에 `N x N` 지도를 순회하며 각 단지를 찾는 과정이 비효율적이라고 생각했지만 지도가 격자로 제공되었기 때문에 모든 점을 확인할 수밖에 없었다. `N <= 25` 조건 때문에 시간복잡도에서도 괜찮았다.
2. BFS 시작하며 `const [curY, curX] = queue.shift();` 부분은 가독성 좋게 `curX, curY`로 설정하는게 좋다.
