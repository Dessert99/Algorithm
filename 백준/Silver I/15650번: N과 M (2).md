[https://www.acmicpc.net/problem/15650](https://www.acmicpc.net/problem/15650)
```javascript
const input = require('fs')
    .readFileSync(process.platform === 'linux' ? '/dev/stdin' : './input.txt')
    .toString()
    .trim()
    .split('\n');

// N: 자연수 범위 , M: 수열 길이. (1~8)
const [N, M] = input[0].split(' ').map(Number);

const arr = Array.from({ length: N }, (_, idx) => idx + 1);

const visited = Array.from({ length: N }, () => false);

let answer = [];

// 재사용할 배열
const tmp = [];

// 조합
function backTracking(start, length) {
    // 기저 조건: 수열 길이가 M일 때.
    if (length === M) {
        // 배열에 있는 요소 조합 후 정답 배열에 넣기
        answer.push(tmp.join(' '));
        return;
    }

    // 기본 조건: 조합 찾기
    for (let i = start; i < N; i++) {
        // 아직 안 넣었다면
        if (visited[i] === false) {
            tmp.push(arr[i]);
            visited[i] = true;

            backTracking(i + 1, length + 1);

            // 빼기
            visited[i] = false;
            tmp.pop();
        }
    }
}
backTracking(0, 0);

console.log(answer.join('\n'));
```
### 🔗 풀이
1. 💡 고른 수열은 오름차순으로 정렬한다. (1, 2)와 (2, 1)은 같은 수열로 취급한다.
    - 조합으로 푼다

2. 👀 방법1)
    1. 조합은 이전에 골랐던 숫자를 다시 고를 필요가 없다.
    2. start 매개변수를 추가해서 반복문을 시작할 위치를 정해준다.
    3. 재귀에서 이전 값은 고려하지 않아도 된다.

### 🔗 배운점
