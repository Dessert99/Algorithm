[https://www.acmicpc.net/problem/2178](https://www.acmicpc.net/problem/2178)

```javascript
const input = require('fs')
    .readFileSync(process.platform === 'linux' ? '/dev/stdin' : './input.txt')
    .toString()
    .trim()
    .split('\n');

let index = 0;

// N: 행, M: 열
const [N, M] = input[index++].split(' ').map(Number);

// 방문 여부 기록 배열
const maps = [];

for (let i = 0; i < N; i++) {
    maps.push(input[index++].split('').map(Number));
}

// 최단 거리 기록 배열
const visited = Array.from({ length: N }, () => Array(M).fill(0));

// 큐
const queue = [];

// 초기값
queue.push([0, 0]);
visited[0][0] = 1;

// dx, dy
const dx = [-1, 0, 1, 0];
const dy = [0, 1, 0, -1];

while (queue.length > 0) {
    // 현재 위치
    const [x, y] = queue.shift();

    // 상하좌우 확인
    for (let i = 0; i < 4; i++) {
        const nx = x + dx[i]; // 현재 위치에서 증가
        const ny = y + dy[i]; // 현재 위치에서 증가

        // 큐에 넣는 조건: 1) 증가한 값이 음수가 아니어야 함 2) 주어진 범위를 넘어가지 않아야 함 3) 유효한 값이어야 함 4) 방문하지 않은 값이어야 함
        if (nx >= 0 && ny >= 0 && nx < N && ny < M && maps[nx][ny] === 1 && visited[nx][ny] === 0) {
            // 큐에 넣기
            queue.push([nx, ny]);
            // 거리 기록. 현재 위치에서
            visited[nx][ny] = visited[x][y] + 1;
        }
    }
}

console.log(visited[N - 1][M - 1]);
```
### 🔗 배운점
1. 다음 좌표(`nx`, `ny`)의 유효성 검사 순서가 중요하다.
   - 이동할 좌표가 격자(Array) 범위를 벗어나는지 가장 먼저 확인해야 한다. 그렇지 않으면 배열의 없는 인덱스에 접근하여 TypeError가 발생할 수 있다
   - 현재 위치 (x, y)에서 증가한 (nx, ny)값이 음수가 되면 안된다. -> `nx >= 0 && ny >= 0`
   - 현재 위치 (x, y)에서 증가한 (nx, ny)값이 주어진 범위 (N, M)을 초과하면 안된다. -> `nx < N && ny < M`
   - 다음 위치를 방문한 적이 있는지 방문 여부를 확인한다.
3. 무한 루프를 방지하기 위해 꼭 방문 여부 조건을 넣자.
