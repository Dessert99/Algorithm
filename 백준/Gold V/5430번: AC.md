[https://www.acmicpc.net/problem/5430](https://www.acmicpc.net/problem/5430)
```javascript
const input = require('fs')
    .readFileSync(process.platform === 'linux' ? '/dev/stdin' : './input.txt')
    .toString()
    .trim()
    .split('\n');

let inputIndex = 0;

// 테스트 케이스 개수 ~100
const T = Number(input[inputIndex++]);

let answer = [];

// 테스트 케이스만큼 반복
for (let t = 0; t < T; t++) {
    // 수행할 함수 목록. 길이: 1~10만 (ex.RDD)
    const p = input[inputIndex++];

    // 배열에 들어있는 수의 개수. 0~10만
    const n = Number(input[inputIndex++]);

    // 정수가 담긴 길이 n 배열. 원소 크기: 1~100
    // [1, 2,..] 형식의 문자열이 오기 때문에 잘 파싱해줘야 한다.
    const arr = input[inputIndex++].slice(1, -1).split(',').map(Number);

    // 방향
    let isReversed = false;

    // 포인터
    let start = 0;
    let end = n - 1;

    let result;

    // 함수 실행
    for (let i = 0; i < p.length; i++) {
        // 실행할 함수: R or D
        const curFunc = p[i];

        // 함수 R일 때
        if (curFunc === 'R') {
            // 시점 바꾸기
            isReversed = !isReversed;
        }

        // 함수 D일 때
        if (curFunc === 'D') {
            // 일단 에러 상황인지 파악하자.
            if (start > end) {
                result = 'error';
                break;
            }

            // 현재 시점에 따라서 다름.
            // 역방향일 때
            if (isReversed) {
                // end값을 줄인다.
                end--;
            } else {
                // 정방향일 때
                // start값을 올린다.
                start++;
            }
        }
    }
    // 이미 error를 출력했을 경우 진행하지 않는다.
    if (result === 'error') {
        answer.push(result);
    } else {
        // 인덱스에 맞게 배열을 자른다.
        const resultArr = arr.slice(start, end + 1);

        // 시점에 따라 reverse
        if (isReversed) {
            resultArr.reverse();
        }

        answer.push('[' + resultArr + ']');
    }
}
console.log(answer.join('\n'));
```
### 🔗 풀이
1. 💡 두 가지 함수
    1. R
        -  배열에 있는 수의 순서를 뒤집는 함수
    2. D
        - 첫 번째 수를 버리는 함수
        - 배열이 비어있는데 D를 사용한 경우에는 에러가 발생

2. 🤔 배열을 뒤집는 방법은 뭐가 있을까? reverse() 메서드
    - reverse 메서드는 호출한 배열을 반전하고 원본 배열을 변형하며 그 참조를 반환한다.
    - 시간복잡도는 O(N)이다.
3. 🤔 p길이만큼 순회하며 reverse를 사용해도 될까?
    - 시간복잡도를 계산해보자. 최악의 경우 p는 길이가 10만이다. 
    - 배열의 길이도 최대 10만이다.
    - 함수가 모두 R로 이루어진 경우 reverse()메서드는 O(N)이기 때문에, 10만x10만 = 100억 번의 연산을 해야한다. 시간 초과 발생.

4. 👀 방법1) R이 연속으로 나오는 경우를 확인하자. 어차피 RR은 바꾸기 전과 동일하다.
    1. 실행할 함수가 R이라면, R이 어디까지 이어져있는지 반복문으로 확인한다.
    2. 이어진 R이 짝수라면 배열을 건드리지 않는다.
    3. 이어진 R이 홀수라면 한 번만 바꾼다.

5. 🤔 함수 D는 어떻게 처리할까?
    - shift()메서드를 사용할 경우 시간복잡도 O(N)이 걸려서 비효율적이다.
    - 큐 클래스를 만들거나, 인덱스 방식을 사용해도, reverse메서드를 사용해야하기 때문에 불가능하다.
    - 👀 방법2) 연속된 D를 세서 slice로 한 번에 분리한다?
        1. R이 연속으로 나오는 상황과 비슷하다.
    - 🤔 그런데 만약 R과 D가 번갈아가면서 나온다면 어떻게 될까?
        - 일단 reverse를 최대 5만 번 실행해야 한다. 이래도 50억 번의 연산을 해야해서 시간 초과가 발생한다.
        - 접근 방법이 달라야할 것 같다.

6. 👀 방법3) 배열의 시점 설정 + 투 포인터
    1. 함수 R이 나왔을 때, 배열을 실제로 뒤집지 않고 시작 시점만 바꾼다.
    2. 함수 D가 나왔을 때, 배열에서 요소를 실제로 제거하는 것이 아니라, start, end 인덱스 값만 조절한다.
    3. q를 전부 확인한 뒤, start,end 범위의 배열만 시작 시점부터 잘라낸다.
    4. start, end가 교차되는 순간 에러가 발생한다.

7. ⭐️ "[1,2,3]" 처럼 배열이 아닌, 문자열로 들어온다.
    - 잘 파싱해서 사용해야 한다.

8. 🚨 예외 상황
    1. 초기 배열이 [1]이고, D만 수행했을 때, start>end이 되지만, 에러 상황이 아니다. error출력 로직을 함수 D 직후로 옮겨준다.


### 🔗 배운점
1. `reverse()`메서드 동작 방식
    
    > reverse 메서드는 호출한 배열을 반전하고 원본 배열을 변형하며 그 참조를 반환한다.
    > 
    - 시간복잡도 : $O(N)$
2. 꼭 뒤집어야 할까?
    
    > 관점을 바꿔봐라. “우리가 보는 시점만 바꾸면 된다.”
    > 
    - 함수 R이 실행될 때, 실제 배열을 바꾸지 말고 현재 시점이 앞인지 뒤인지 저장할 변수를 하나 만들어 놓자.
3. 투 포인터
    
    > “연속된 데이터”나 "정렬된 데이터"를 다룰 때 반복문을 두 번 중첩(`for` 안에 `for`)해야 할 것 같은 상황에서 사용한다.
    > 
    1. 패턴 A: 양쪽에서 조여오기
        
        > 주로 "정렬된 배열"에서 두 수의 합을 찾거나, 문자열을 뒤집을 때 사용
        > 
        
        ```jsx
        function twoPointersConverging(arr, target) {
            let start = 0;
            let end = arr.length - 1;
        
            while (start < end) { // 서로 만날 때까지 반복
                const sum = arr[start] + arr[end];
        
                if (sum === target) {
                    return [start, end]; // 찾았다!
                } 
                else if (sum < target) {
                    start++; // 합이 작으면 -> 작은 쪽(왼쪽)을 키움
                } 
                else {
                    end--;   // 합이 크면 -> 큰 쪽(오른쪽)을 줄임
                }
            }
            return null;
        }
        ```
        
        - `start`는 0에서, `end`는 끝에서 시작해서 서로 만날 때까지 좁혀온다.
    2. 패턴 B: 같은 방향으로 슬라이딩
        
        > 주로 "연속된 부분 배열의 합이나 길이"를 구할 때 사용
        > 
        
        ```jsx
        function twoPointersSliding(arr, targetSum) {
            let start = 0;
            let end = 0;
            let currentSum = 0;
            let minLength = Infinity;
        
            // end가 끝까지 갈 때까지
            while (end < arr.length) {
                // 1. 일단 end를 증가시키며 영역 확장
                currentSum += arr[end];
                end++;
        
                // 2. 조건(합이 목표보다 커짐)을 만족하면 start를 당겨서 영역 축소
                while (currentSum >= targetSum) {
                    minLength = Math.min(minLength, end - start);
                    
                    currentSum -= arr[start];
                    start++;
                }
            }
            return minLength;
        }
        ```
        
        - `start`와 `end` 둘 다 0에서 시작합니다. `end`가 먼저 앞으로 가면서 영역을 넓히고, 조건에 따라 `start`가 따라가며 영역을 줄인다.
