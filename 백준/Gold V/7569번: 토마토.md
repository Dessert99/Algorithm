[https://www.acmicpc.net/problem/7569](https://www.acmicpc.net/problem/7569)
```javascript
const input = require('fs')
    .readFileSync(process.platform === 'linux' ? '/dev/stdin' : './input.txt')
    .toString()
    .trim()
    .split('\n');

let inputIndex = 0;

// M: 가로(2~100) , N: 세로(2~100), H: 높이(1~100)

const [M, N, H] = input[inputIndex++].split(' ').map(Number);

// 토마토 정보
const maps = [];

for (let i = 0; i < H; i++) {
    // 1개 층만 잘라낸다.
    const floor = input.slice(inputIndex, inputIndex + N);

    // 가로-세로를 잘라서 상자에 저장
    maps.push(floor.map((row) => row.split(' ').map(Number)));

    inputIndex += N;
}

// 초기값으로 익은 토마토 넣기. [층, 세로, 가로]
const queue = [];
let head = 0;

for (let z = 0; z < H; z++) {
    for (let y = 0; y < N; y++) {
        for (let x = 0; x < M; x++) {
            if (maps[z][y][x] === 1) {
                queue.push([z, y, x]);
            }
        }
    }
}

// 상하좌우
const dy = [-1, 0, 1, 0];
const dx = [0, 1, 0, -1];

// 위아래
const dz = [-1, 1];

// BFS시작
while (queue.length > head) {
    // 익은 토마토 꺼내기
    const [curZ, curY, curX] = queue[head++];

    // 현재 시간대 (초기값 1)
    const curTime = maps[curZ][curY][curX];

    // 상하좌우 먼저
    for (let i = 0; i < 4; i++) {
        const ny = curY + dy[i];
        const nx = curX + dx[i];

        // 조건: 같은 층에서 안익은 토마토
        if (ny < N && nx < M && ny >= 0 && nx >= 0 && maps[curZ][ny][nx] === 0) {
            // 큐에 넣기
            queue.push([curZ, ny, nx]);
            // 익음 표시 + 시간대도 저장
            maps[curZ][ny][nx] = curTime + 1;
        }
    }

    // 위아래 확인
    for (let i = 0; i < 2; i++) {
        const nz = curZ + dz[i];

        if (nz < H && nz >= 0 && maps[nz][curY][curX] === 0) {
            queue.push([nz, curY, curX]);
            maps[nz][curY][curX] = curTime + 1;
        }
    }
}

// 지도에 0이 남아있다면 -1 출력한다. 모든값이 1이라면 0을 출력한다. 이외에는 가장 높은 숫자 -1 을 출력한다.

function checkTomato() {
    let answer = 1;

    for (let z = 0; z < H; z++) {
        for (let y = 0; y < N; y++) {
            for (let x = 0; x < M; x++) {
                // 0이 남아있다면 -1 출력
                if (maps[z][y][x] === 0) {
                    return -1;
                } else {
                    // 그 외의 경우 더 높은 숫자로 교체한다.
                    answer = Math.max(answer, maps[z][y][x]);
                }
            }
        }
    }
    return answer;
}

const result = checkTomato();
if (result === -1) {
    console.log(-1);
} else if (result === 1) {
    console.log(0);
} else {
    console.log(result - 1);
}
```
### 🔗 풀이
1. 💡 최대 지도 크기
    - 가로x세로x높이 = 백만

2. 👀 방법1) 2차원 토마토 문제와 유사
    1. 초기값을 큐에 익은 토마토 전부 넣기
    2. 턴마다 근처 토마토 전부 넣기
    3. 방문 여부 배열은 필요 없음.

3. 🤔 입체적인 부분을 코드로 어떻게 구현할까?
    - 3차원 배열로 만든다.

4. 🤔 dy, dx를 어떻게 표현할까?
    - 일단 기존 dy, dx 그대로 쓰고,
    - 추가로 dz를 만들까?
### 🔗 배운점
1. 2차원 토마토 문제가 3차원으로 바뀐 것 뿐이다.
   - 퍼지는 로직은 동일하다. 상하좌우를 확인하는 로직에 위아래를 확인하는 로직만 추가하면 된다.
