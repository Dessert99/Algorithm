[https://www.acmicpc.net/problem/16928](https://www.acmicpc.net/problem/16928)
```javascript
const input = require('fs')
    .readFileSync(process.platform === 'linux' ? '/dev/stdin' : './input.txt')
    .toString()
    .trim()
    .split('\n');

let inputIndex = 0;

// N: 사다리의 수, M: 뱀의 수,  1~15
const [N, M] = input[inputIndex++].split(' ').map(Number);

// 사다리, 맵 해시맵
const ladderMap = new Map();
const snakeMap = new Map();

for (let i = 0; i < N; i++) {
    const [start, end] = input[inputIndex++].split(' ').map(Number);
    ladderMap.set(start, end);
}

for (let i = 0; i < M; i++) {
    const [start, end] = input[inputIndex++].split(' ').map(Number);
    snakeMap.set(start, end);
}

// 방문 여부 배열. 편의성을 위해 길이를 1 늘린다.
const visited = Array.from({ length: 101 }, () => false);

//큐
const queue = [];
let head = 0;

// 초기값 삽입: [내 위치, 주사위 굴린 횟수]
queue.push([1, 0]);
visited[1] = true;

// 최단 거리
let answer;

while (queue.length > head) {
    // 큐에서 현재 위치, 누적값 꺼내기
    const [curPos, count] = queue[head++];

    // 종료 조건: 100에 도달 했을 때
    if (curPos >= 100) {
        answer = count;
        break;
    }

    // 주사위 돌리기
    for (let i = 1; i <= 6; i++) {
        // 이동 위치
        let nextPos = curPos + i;

        // 처음 가본 곳이어야 함.
        if (visited[nextPos] === false) {
            // 방문 표시
            visited[nextPos] = true;

            // 이동할 위치가 사다리라면
            if (ladderMap.has(nextPos)) {
                // 사다리를 타고 올라간다.
                nextPos = ladderMap.get(nextPos);
                //방문 표시
                visited[nextPos] = true;
                //큐에 넣는다.
                queue.push([nextPos, count + 1]);
            } else if (snakeMap.has(nextPos)) {
                // 이동할 위치가 뱀이라면 내려간다.
                nextPos = snakeMap.get(nextPos);
                // 방문 표시
                visited[nextPos] = true;
                // 큐에 넣는다.
                queue.push([nextPos, count + 1]);
            } else {
                // 평범한 발판이라면
                queue.push([nextPos, count + 1]);
            }
        }
    }
}

console.log(answer);
```
### 🔗 풀이
1. 🤔 뱀이라고 꼭 피해야 할까?
    - 오히려 뱀을 밟아야 더 빨리 도착하는 경우가 있다.
    - 뱀만 피해다닐게 아니라, 모든 경우의 수를 확인해보자.

2. 🤔 BFS로 푼다면 모든 주사위 수를 고려해야할까?
    - 지도의 최대 길이는 100이다.
    - 방문 여부 배열을 만든다면 모든 위치는 한 번씩만 갈 수 있다.
    - 정확한 시간복잡도 계산은 모르겠다. 100 x 5 x 4 x 3 x 2 x 1 인가?
      - 아니다. 자세한 풀이는 배운점 섹션을 참고하자.

3. 🤔 큐에 넣어야 할 정보는 무엇일까?
    1. 현재 위치 정보
    2. 주사위를 굴린 누적 횟수

4. 🤔 사다리, 뱀 정보는 어디에 넣을까?
    1. 해시맵
        - 위치를 옮길 때마다 확인한다. 비용은 그렇게 크지 않을듯.

### 🔗 배운점
1. 시간 복잡도 계산
    
    > 시간복잡도는 `100 x 5 x 4…`가 아니라,  $100 \times 6$ 이다.
    > 
    - BFS의 시간 복잡도 공식은 **$O(V + E)$** 이다.
        - **$V$ (정점):** 칸의 개수 (1번 ~ 100번) $\rightarrow$ **100개**
        - **$E$ (간선):** 이동 가능한 경로의 수
    - 이 문제에서 $E$를 계산해보자.
        - 각 칸(정점)에서 주사위 1~6을 굴려 갈 수 있으니, 간선은 최대 6개다.
            - **총 연산량:** $V(100) + E(100 \times 6) = 700$번.
    - 핵심:  `visited` 배열 덕분에 우리는 이미 방문한 칸을 다시 계산하지 않는다.
