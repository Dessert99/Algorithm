[https://www.acmicpc.net/problem/2230](https://www.acmicpc.net/problem/2230)
```javascript
const input = require('fs')
    .readFileSync(process.platform === 'linux' ? '/dev/stdin' : './input.txt')
    .toString()
    .trim()
    .split('\n');

let inputIndex = 0;

// N: 최대 10만개
const [N, M] = input[inputIndex++].split(' ').map(Number);

const arr = [];
for (let i = 0; i < N; i++) {
    arr.push(Number(input[inputIndex++]));
}
arr.sort((a, b) => a - b);

let left = 0;
let right = 0;

let answer = Number.MAX_SAFE_INTEGER;

while (right < N) {
    // 차이를 구한다.
    const gap = arr[right] - arr[left];

    // 만약 차이가 M과 같다면 종료
    if (gap === M) {
        answer = gap;
        break;
    }

    // 만약 M보다 작다?
    if (gap < M) {
        // right를 키운다.
        right++;
    } else {
        // M 조건에 만족한다.
        // 최솟값 갱신
        answer = Math.min(answer, gap);

        // 여기서 left를 늘린다.
        left++;
    }
}

console.log(answer);
```
### 🔗 풀이
1. 🤔 우리가 해야할 일.
    1. 어떤 수를 선택한다.
    2. 다른 N-1 수와 비교한다.
    3. 그걸 N번 반복한다.
    - 하지만 이렇게 하면 10만 제곱번 연산이 필요하기 때문에 시간 초과가 발생한다.

2. 🤔 정렬을 활용한다면?
    - 순서대로 정렬하면 수들의 차이를 어느정도 예상할 수 있게 된다.

3. 🤔 투포인터를 쓴다면?
    - 한 쪽을 기본으로 움직이고, 어떤 조건을 달성했을 때만 나머지 한 쪽을 움직여보자.
    - 그 조건은?

### 🔗 배운점
1. 포인터를 어디에 두어야 할지 판단 근거
    - 양 끝에 두는 것은 합을 구할 때이다.
    - 두 수의 차이를 구하기 위해서는 처음부터 함께 시작해야 한다.
2. 투 포인터 종류
     1. **"두 원소의 합" OR "정렬된 배열에서 쌍(Pair) 찾기"**
        > **양끝 포인터 (`left = 0, right = N - 1`)**
        - 정렬된 상태에서 양끝을 좁혀오면, 합을 키우거나 줄이는 방향성이 완벽히 보장됩니다.
      2. **"연속된 부분 배열의 합/길이" OR "두 수의 차이"**
            > **같은 곳 출발 (`left = 0, right = 0`)**
            - 조건(합이나 차이)이 부족하면 `right`를 늘려 파이를 키우고, 조건을 넘치게 만족하면 `left`를 당겨서 타이트하게 최적화합니다. 슬라이딩 윈도우와 호흡이 같습니다.
      3. **"두 개의 다른 배열을 비교/병합"**
            > **각 배열의 시작점 (`p1 = 0, p2 = 0`)**
            - 두 배열이 이미 정렬되어 있을 때, 둘 중 더 작거나 조건에 맞는 쪽의 포인터만 전진시킵니다.
3. 투 포인터를 만났을 때 사고 과정
   - 구해야 하는 값이 원소들의 합인지 차이인지 확인한다.
   - 정렬이 허용되어 원소의 순서가 바뀌어도 되는지, 원본 배열의 연속성이 유지되어야 하는지 파악한다.
   - 포인터가 이동할 때의 명확한 if분기 조건을 세워야 한다.
4. 실수 잡기
   1. 정렬 판단 근거 중요함
   2. 같은 곳 출발하면 보통  `while`문을 사용한다. 이때, 종료 조건을 `right`가 끝에 닿자마자 해버리면 안된다.
      - `right`가 끝에 닿앗어도, `left`를 계속 당겨오면서 답을 구할 수도 있다.
   3. 양끝 포인터에서 `left <= right`, `left < right` 중 어떻게 할지 판단해야 한다.
      - 두 수의 합은 `<`이지만, 한 원소도 헝요되면 `<=`이다.
