[https://www.acmicpc.net/problem/7576](https://www.acmicpc.net/problem/7576)

```javascript
const input = require('fs')
    .readFileSync(process.platform === 'linux' ? '/dev/stdin' : './input.txt')
    .toString()
    .trim()
    .split('\n');

let index = 0;

// M 가로, N 세로
const [M, N] = input[index++].split(' ').map(Number);

// 시간 기록 배열
const timeCheck = [];

// 초기 상자
const maps = [];
for (let i = 0; i < N; i++) {
    maps.push(input[index].split(' ').map(Number));
    timeCheck.push(input[index].split(' ').map(Number));
    index++;
}

// 방문 여부 배열
const visited = Array.from({ length: N }, () => Array(M).fill(false));

// BFS 준비물
const dx = [-1, 0, 1, 0];
const dy = [0, 1, 0, -1];

// 큐
class Queue {
    constructor() {
        this.store = [];
        this.front = 0; // 맨 앞 포인터 (빼는 곳)
        this.rear = 0; // 맨 뒤 포인터 (넣는 곳)
    }

    size() {
        return this.rear - this.front;
    }

    //데이터 넣기
    enqueue(value) {
        this.store[this.rear] = value; // 맨 뒤에 넣고
        this.rear++; // 인덱스 증가
    }

    //데이터 빼기
    dequeue() {
        // 큐가 비었다면 undefined 반환
        if (this.front === this.rear) {
            return undefined;
        }
        const value = this.store[this.front];
        this.front++; // 맨 앞 포인터 이동
        return value;
    }
}

// 내가 만든 큐
const q = new Queue();

// 맨 처음 익은 토마토를 큐에 넣기
for (let y = 0; y < N; y++) {
    for (let x = 0; x < M; x++) {
        // 익은 토마토는 큐에 넣기
        if (maps[y][x] === 1) {
            q.enqueue([y, x]);
            visited[y][x] = true;
        }
    }
}

// 탐색 시작
while (q.size() > 0) {
    // 현재 토마토 위치
    const [y, x] = q.dequeue();

    // 상하좌우 확인
    for (let n = 0; n < 4; n++) {
        const ny = y + dy[n];
        const nx = x + dx[n];

        // 큐에 넣을 조건 1) 인덱스 유효 2) 안익은 토마토  3) 방문하지 않은 토마토
        if (ny >= 0 && nx >= 0 && ny < N && nx < M && maps[ny][nx] === 0 && visited[ny][nx] === false) {
            visited[ny][nx] = true; // 방문 체크
            timeCheck[ny][nx] = timeCheck[y][x] + 1; // 현재 위치에서 + 1초
            maps[ny][nx] = 1; // 토마토 익음
            q.enqueue([ny, nx]); // 방문 예정
        }
    }
}

let answer = 0;

// 만약 시간 체크 배열에서 아직 0초인 토마토가 있다는 것은, 벽에 막혀서 토마토가 익지 못한다는 것이다.
timeCheck.forEach((row) => {
    if (row.some((i) => i === 0)) {
        answer = -1;
    }
});

// 만약 모든 토마토가 익었다면 가장 오래 걸린 시간에서 -1 한 값이 정답이다.
if (answer !== -1) {
    for (let i = 0; i < N; i++) {
        for (let j = 0; j < M; j++) {
            if (answer < timeCheck[i][j]) {
                answer = timeCheck[i][j];
            }
        }
    }
    answer--;
}

console.log(answer);
```
### 🔗 배운점
1. 시간 복잡도를 고려해 큐(Queue)를 직접 구현하자.
   - 자바스크립트의 shift()는 $O(N)$이라 연산 비용이 비싸다. 데이터가 최대 100만 개까지 늘어날 수 있는 이번 문제에서 shift()를 쓰면 시간 초과가 발생한다. 큐를 직접 구현하면 $O(1)$로 맨 앞 원소를 효율적으로 처리할 수 있다.
2. 별도의 방문 배열(visited)이 꼭 필요한지 고민하자.
   - 이번 문제에서는 maps[ny][nx] === 0 조건으로 방문 여부를 확인할 수 있어 굳이 visited 배열을 만들 필요가 없다. 또한, 경과 시간도 maps에 직접 기록하면 되므로 시간 체크용 배열도 생략 가능하다. 기존 배열을 활용해 메모리를 아끼자.
