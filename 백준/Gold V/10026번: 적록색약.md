[https://www.acmicpc.net/problem/10026](https://www.acmicpc.net/problem/10026)
```javascript
const input = require('fs')
    .readFileSync(process.platform === 'linux' ? '/dev/stdin' : './input.txt')
    .toString()
    .trim()
    .split('\n');

let inputIndex = 0;

// 그리드 크기
const N = Number(input[inputIndex++]);

// 정상인 maps
const maps = [];
for (let i = 0; i < N; i++) {
    maps.push(input[inputIndex++].split(''));
}

// 색약 전용 maps
const colorBlindnessMaps = maps.map((row) => row.map((i) => (i === 'R' ? 'G' : i)));

const dy = [-1, 0, 1, 0];
const dx = [0, 1, 0, -1];

// maps를 넣으면 구역 개수를 반환하는 함수
function CountArea(area) {
    // 구역의 수
    let count = 0;

    // 방문 여부 배열
    const visited = Array.from({ length: N }, () => Array(N).fill(false));

    // 큐
    const queue = [];
    let head = 0;

    // 순차 탐색
    for (let y = 0; y < N; y++) {
        for (let x = 0; x < N; x++) {
            // 이미 확인한 곳이라면 패스
            if (visited[y][x] === true) {
                continue;
            }

            // 초기값 세팅: 위치와 함께 구역 타입도 함께 묶는다.
            queue.push([y, x, area[y][x]]);
            visited[y][x] = true;

            // 같은 구역 탐색
            while (queue.length > head) {
                const [curY, curX, curType] = queue[head++];

                // 상하좌우 확인
                for (let i = 0; i < 4; i++) {
                    const ny = curY + dy[i];
                    const nx = curX + dx[i];

                    // 큐에 들어가는 조건 1) 유효한 값 2) 방문 X 3) 같은 타입
                    if (
                        ny >= 0 &&
                        nx >= 0 &&
                        ny < N &&
                        nx < N &&
                        visited[ny][nx] === false &&
                        area[ny][nx] === curType
                    ) {
                        // 큐에 넣고 방문 체크
                        queue.push([ny, nx, area[ny][nx]]);
                        visited[ny][nx] = true;
                    }
                }
            }
            // 구역 카운팅
            count++;
        }
    }
    return count;
}
const normalCount = CountArea(maps);
const colorBlindnessCount = CountArea(colorBlindnessMaps);

console.log(`${normalCount} ${colorBlindnessCount}`);
```
### 🔗 풀이
1. 정상인, 적록색약 각각 구역을 확인한다.

### 🔗 배운점
1. 2차원 배열 복사하며 수정하기
    
    ```jsx
    // maps.map(줄 => 줄.map(칸 => 조건 ? 바꿀값 : 원래값))
    const colorBlindnessMaps = maps.map(row => 
        row.map(cell => (cell === 'R' ? 'G' : cell))
    );
    ```
    
    - 시각화
        
        ```jsx
        [
          ['R', 'G'], 
          ['R', 'B']
        ]
        ```
        
        ⬇️ **1단계: 바깥쪽 map (행 단위 접근)**
        
        - `row`: `['R', 'G']`
        ⬇️ **2단계: 안쪽 map (칸 단위 접근 & 수정)**
        - `cell`: `'R'` -> `'G'`로 변환
        - 결과: `['G', 'G']`, `['G', 'B']` 구조가 그대로 유지됨.
