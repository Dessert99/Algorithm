[https://www.acmicpc.net/problem/14503](https://www.acmicpc.net/problem/14503)
```javascript
const input = require('fs')
    .readFileSync(process.platform === 'linux' ? '/dev/stdin' : './input.txt')
    .toString()
    .trim()
    .split('\n');

let inputIndex = 0;

// N: 세로, M: 가로 (3~50)
const [N, M] = input[inputIndex++].split(' ').map(Number);

// r: 로봇 청소기 행 위치, c: 로봇 청소리 열 위치, d: 로봇 청소기 방향
const [r, c, d] = input[inputIndex++].split(' ').map(Number);

// 방
const rooms = [];
for (let i = 0; i < N; i++) {
    rooms.push(input[inputIndex++].split(' ').map(Number));
}

// 현재 로봇 위치 및 방향
let curY = r;
let curX = c;
let curD = d;

// 후진 위치 (남 서 북 동)
const by = [1, 0, -1, 0];
const bx = [0, -1, 0, 1];

// 0탐색방향 (북 동 남 서)
const fy = [-1, 0, 1, 0];
const fx = [0, 1, 0, -1];

let answer = 0;

// 로봇이 멈출 때까지 반복
while (true) {
    // 현재 위치가 0이라면 청소
    if (rooms[curY][curX] === 0) {
        rooms[curY][curX] = 2; // 청소 했으면 2
        answer++;
    }

    // 주변 4칸에 0이 있는지 확인한다.
    let checked = false;
    for (let i = 0; i < 4; i++) {
        // 반시계 회전 (북0 -> 서3)
        curD = (curD - 1 + 4) % 4;

        // 다음 위치
        const frontY = curY + fy[curD];
        const frontX = curX + fx[curD];

        // 유효한지 확인
        if (frontX >= 0 && frontY >= 0 && frontY < N && frontX < M && rooms[frontY][frontX] === 0) {
            // 가능 체크
            checked = true;
            // 앞으로 이동
            curY = frontY;
            curX = frontX;

            // 방향 확인 종료
            break;
        }
    }

    // 0이 없다면
    if (!checked) {
        // 후진 위치: 바라보고 있는 방향 반대
        const backY = curY + by[curD];
        const backX = curX + bx[curD];

        // 후진하는 조건1) 유효한 위치 2) 벽이 아닌 칸
        if (backY >= 0 && backX >= 0 && backY < N && backX < M && rooms[backY][backX] !== 1) {
            // 후진했다. 위치 갱신
            curX = backX;
            curY = backY;
        } else {
            // 후진하지 못하는 상황
            break;
        }
    }
}

console.log(answer);
```
### 🔗 풀이
1. 💡 방향 개념이 존재한다.
    - 0인 경우 북쪽
    - 1인 경우 동쪽
    - 2인 경우 남쪽
    - 3인 경우 서쪽

2. 💡 주변 4칸에 0이 있는 경우
    - 바라보는 방향이 0값이 될 때까지 반시계로 회전한다. 그리고 앞으로 전진하여 청소한다.
    - 주변에 0이 많다면, 반시계로 돌았을 때 가장 가까운 곳으로 이동한다.

3. 💡 주변 4칸에 0이 없는 경우
    - 후진한다.
    - 후진 못하면 종료
4. 🤔 어떤 알고리즘을 써야 할까?
    - BFS는 아닌 것 같다. 상하좌우 확인하는 로직은 있어야 하지만 이동하는 경우가 한가지이다.
    - 👀 방법1) 단순 반복문
        1. 현재 로봇 위치를 변수에 저장한다.
        2. while반복
        3. 상하좌우를 확인해서 0이 있는지 확인한다.
        4. 0이 있다면 그 방향을 처음 마주할 때까지 회전하고 전진하고 청소한다.
            - 전진하여 청소했다면 로봇 위치를 갱신한다.
        5. 0이 없다면 후진한다. 만약 후진할 수 없다면 종료한다.
            - 후진했다면 로봇 위치를 갱신한다.

5. 🤔 후진한다는 것은?
    - 현재 북쪽을 바라보고 있다면 (curD = 0)
    - 남쪽으로 이동하는 것이다. (curX 그대로, curY += 1)
### 🔗 배운점
