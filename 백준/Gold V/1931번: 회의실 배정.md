[https://www.acmicpc.net/problem/1931](https://www.acmicpc.net/problem/1931)
```javascript
const input = require('fs')
    .readFileSync(process.platform === 'linux' ? '/dev/stdin' : './input.txt')
    .toString()
    .trim()
    .split('\n');

let inputIndex = 0;

// 회의의 수. 1~10만
const N = Number(input[inputIndex++]);

// 회의 배열
const meetings = [];
for (let i = 0; i < N; i++) {
    meetings.push(input[inputIndex++].split(' ').map(Number));
}

// 종료 시간 기준으로 오름차순 정렬한다. 종료 시간이 같다면 시작 시간 기준으로 오름차순 정렬한다.
const sortedMeetings = meetings.sort((a, b) => a[1] - b[1] || a[0] - b[0]);

// 회의 개수
let count = 0;

// 회의 종료 시간
let curEnd = 0;

for (let i = 0; i < N; i++) {
    // 현재 회의 시작/종료 시간
    const [start, end] = sortedMeetings[i];

    // 시작 시간이 최근 종료 시간 이후인지 확인한다.
    if (start >= curEnd) {
        // 사용 가능하다.
        count++;

        // 현재 종료 시간을 저장
        curEnd = end;
    }
}
console.log(count);
```
### 🔗 풀이
💡 우리에게 주어진 정보
    1. 총 회의의 수
    2. 각 회의의 시작, 끝 시간

👀 방법1) (설명 필요))
    1. 끝나는 시간 기준 오름차순으로 정렬한다.
    2. 첫 번째 요소가 가장 빨리 끝나는 회의다.
    3. 첫 번째 회의가 끝나고 바로 회의를 진행할 수 있는 시간을 찾는다.
    4. 그 회의가 가장 효율적인 선택이다.

### 🔗 배운점
1. `Array.prototype.sort()`
    1. 기본 동작
        
        ```jsx
        const nums = [1, 2, 10, 25];
        
        nums.sort(); 
        console.log(nums); // 🚨 결과: [1, 10, 2, 25]
        ```
        
        - `sort()` 안에 아무것도 넣지 않으면, 자바스크립트는 모든 요소를 문자열로 변환한 뒤 유니코드(ASCII) 순서대로 비교한다. 문자열 “10”은 “2”보다 빠르기 떄문에 예상 결과와 다르다.
    2. 비교 함수 `(a, b) => ...`
        
        ```jsx
        arr.sort((a, b) => {
          // 리턴값에 따라 자리를 바꿈
          return 결과값;
        });
        ```
        
        - 숫자를 원하는대로 정렬하려면 비교 함수를 넣어야 한다.
    3. 비교 함수 리턴값
        
        
        | **리턴값** | **의미** | **동작 (오름차순 기준)** | **비유** |
        | --- | --- | --- | --- |
        | **음수** (`-`) | `a > b` | **`a`를 앞으로 보냄** | "a가 가벼우니 앞으로 가!" |
        | **양수** (`+`) | `a < b` | **`b`를 앞으로 보냄** (`a`를 뒤로) | "a가 무거우니 뒤로 가!" |
        | **0** | 같다 | **자리 안 바꿈** | "너네 둘은 동급이야." |
        - 비교 함수의 리턴값의결과에 따라 정렬이 달라진다.
2. `sort()` 기본 템플릿
    1. 숫자 정렬
        
        ```jsx
        const nums = [3, 1, 10, 5];
        
        // 오름차순 (1, 3, 5, 10)
        nums.sort((a, b) => a - b);
        
        // 내림차순 (10, 5, 3, 1)
        nums.sort((a, b) => b - a);
        ```
        
    2. 문자열 정렬 (사전순)
        
        ```jsx
        const strs = ["banana", "apple", "carrot"];
        
        // 오름차순 (apple, banana, carrot)
        // 방법 1: 가장 빠르고 일반적 (ASCII 기준)
        strs.sort(); 
        // 방법 2: 명시적 비교 (추천)
        strs.sort((a, b) => (a > b ? 1 : -1));
        // 방법 3: 한글 등 다국어 정렬 시 필수
        strs.sort((a, b) => a.localeCompare(b)); 
        
        // 내림차순 (carrot, banana, apple)
        strs.sort((a, b) => (a < b ? 1 : -1));
        strs.sort((a, b) => b.localeCompare(a));
        ```
        
        - 자바스크립트 문자열은 빼기(`-`)가 안 되므로 비교 연산자나 `localeCompare`를 쓴다.
    3. 객체 정렬 (다중 조건)
        
        ```jsx
        const users = [
            { name: "철수", age: 20, score: 90 },
            { name: "영희", age: 20, score: 85 },
            { name: "민수", age: 25, score: 90 },
        ];
        
        // 정렬 우선순위:
        // 1순위: 나이 (오름차순)
        // 2순위: 점수 (내림차순, 나이가 같다면)
        // 3순위: 이름 (가나다순, 나이와 점수가 같다면)
        
        users.sort((a, b) => {
            return (a.age - b.age) ||          // 1. 나이 비교 (다르면 여기서 리턴됨)
                   (b.score - a.score) ||      // 2. 점수 비교 (나이 같으면 실행) -> 내림차순이라 b - a
                   (a.name.localeCompare(b.name)); // 3. 이름 비교 (점수도 같으면 실행)
        });
        ```
        
3. `sort()` 는 원본 배열을 변형한다.
    
    ```jsx
    const original = [3, 1, 2];
    const sorted = original.sort();
    
    console.log(original); // [1, 2, 3] -> 😱 원본이 바뀌어버림!
    ```
    
    - 원본을 냅두고 새로운 배열을 받고 싶다면: `toSorted()`
        
        ```jsx
        // 1. 구식 방법 (복사 후 정렬)
        const sorted1 = [...original].sort((a, b) => a - b);
        
        // 2. 신식 방법 (toSorted) - Node.js 20+, 최신 브라우저
        const sorted2 = original.toSorted((a, b) => a - b);
        ```
        
4. `sort()`는 TimSort알고리즘을 사용한다.
    
    > 합병 정렬와 삽입 정렬을 섞은 하이브리드 알고리즘
    > 
    - **시간 복잡도**
        - 최악: $O(N \log N)$ (퀵 정렬보다 안전함)
        - 최선: $O(N)$ (이미 정렬된 경우)
5. `sort()` 는 안정 정렬이다.
    - 정렬 기준값(점수 등)이 같으면, 원래 순서가 유지된다.
