[https://www.acmicpc.net/problem/9019](https://www.acmicpc.net/problem/9019)
```javascript
const input = require('fs')
    .readFileSync(process.platform === 'linux' ? '/dev/stdin' : './input.txt')
    .toString()
    .trim()
    .split('\n');

let inputIndex = 0;

// 테스크 케이스 개수
const T = Number(input[inputIndex++]);

const answer = [];

// 모듈로 기준
const mod = 10000;

// 명령어 함수
const command_D = (n) => {
    return (n * 2) % mod;
};

const command_S = (n) => {
    return (n - 1 + mod) % mod;
};

const command_L = (n) => {
    const front = Math.floor(n / 1000); // 맨 앞자리 구하기
    const back = n % 1000; // 뒷부분 구하기
    return back * 10 + front; // 합치기
};

const command_R = (n) => {
    const back = n % 10; // 맨 뒷자리 구하기
    const front = Math.floor(n / 10); // 앞부분 구하기
    return back * 1000 + front; // 합치기
};

// 명령어 배열
const commands = [
    { name: 'D', cmd: command_D },
    { name: 'S', cmd: command_S },
    { name: 'L', cmd: command_L },
    { name: 'R', cmd: command_R },
];

// 각 테스트 케이스
for (let t = 0; t < T; t++) {
    // A,B  0~1만
    let [A, B] = input[inputIndex++].split(' ').map(Number);

    // 큐
    const q = [];
    let head = 0;

    // 방문 여부 배열 0~1만 숫자를 확인한다.
    const visited = Array.from({ length: 10000 }, () => false);

    // 초기값: [현재값, 명령어 배열]
    q.push([A, '']);

    // BFS 시작
    while (q.length > head) {
        // 현재값, 누적 명령어
        const [curNum, curPath] = q[head++];

        // 종료 조건: B와 동일할 때
        if (curNum === B) {
            // 명령어 정답 배열에 넣기
            answer.push(curPath);

            break;
        }

        // 명령어
        for (const command of commands) {
            const name = command.name;
            const cmd = command.cmd;

            // 명령어 입력
            const nextNum = cmd(curNum);

            // 처음 나온 숫자일 때만 큐에 넣는다.
            if (visited[nextNum] === false) {
                visited[nextNum] = true;
                q.push([nextNum, curPath + command.name]);
            }
        }
    }
}

console.log(answer.join('\n'));
```
### 🔗 풀이
1. 💡 명령어 정리
    1. D
        - 2배
        - 9999 보다 큰 경우에는 10000 으로 나눈 나머지
    2. S
        - 1빼기
        n이 0 이라면 9999 대신 저장
    3. L
        - 1234 -> 2341
        - 1000 -> 0001 = 1
    4. R
        - 1234 -> 4123
        - 1000 -> 0100 = 100

2. 🤔 명령어 선택 기준은 어떻게 될까? 
    - 사람은 A, B를 보면 어떻게 할지 판단할 수 있지만 컴퓨터는 그렇지 못하다.
    - 4가지 명령어를 모두 해보는 건 어떨까?
        - 🤔 연산이 너무 많아지지는 않을까? 최약의 경우를 살펴보자.
            A: 9999
            B: 1
            일때 S명령어만 사용한다면 9998번 사용해야 한다.
            하지만 BFS로 4가지 명령어를 섞어서 사용한다면 그보다는 빨리 끝날 것 같다. (정확히 얼마나 빨리 끝나는지는 모르겠음)

3. 👀 방법1) 원하는 결과가 나올 떄까지 BFS 탐색
    🤔 누적 명령어는 어떻게 기억할 것인가?
        - 큐에 묶는다.
    🤔 큐에 넣을 정보는 무엇인가?
        1. 현재값
        2. 누적 명령어
        🤔 메모리 걱정은 안해도 되는가?
            - 상태의 개수(정점): 10,000개
            - 간선의 개수: 각 정점당 4개 (D, S, L, R)
            - 아무리 많아봤자 연산 횟수는 수만~수십만 번 수준이다. 컴퓨터에게는 식은 죽 먹기다. (보통 1억 번 연산이 1초 정도)
    🤔 방문 여부 배열이 필요한가?
        - 필요하다. 기록하지 않으면 이미 나왔던 숫자가 계속 나온다.
        - 어짜피 최대 1만개 숫자이니, 길이 1만의 visited배열을 만든다.

### 🔗 배운점
1. 누적 명령어는 배열이 아닌, "문자열"로 하자.
    - 최대 1만번밖에 안되고, 배열은 참조 문제 때문에 복잡해진다.
    - 문자열을 산술 연산자로 계산하면 새로운 문자열이 되기 때문에 깔끔해진다.
2. 모듈러 연산과 `/` 연산 템플릿
    1. 자릿수 쪼개기 (기본기)
        
        ```jsx
        const n = 1234;
        
        // 1. 맨 끝자리 구하기 (1의 자리) -> % 10
        const lastDigit = n % 10; 
        // 결과: 4
        
        // 2. 맨 끝자리 버리기 (앞부분만 남기기) -> / 10
        const rest = Math.floor(n / 10); 
        // 결과: 123
        ```
        
    2. 자릿수 회전
        
        > 4자리 숫자(`0000` ~ `9999`)를 기준으로 숫자를 뱅글뱅글 돌리는 공식
        > 
        - 왼쪽 회전 (L): `1234` ➔ `2341`
            
            ```jsx
            // n = 1234 라고 가정
            
            // [1단계] 맨 앞자리(천의 자리) 구하기
            const front = Math.floor(n / 1000); // 1
            
            // [2단계] 나머지 뒷부분 구하기
            const back = n % 1000; // 234
            
            // [3단계] 합치기
            // 뒷부분(234)을 한 칸 앞으로 당기고(* 10) + 맨 앞자리(1)를 뒤에 붙임
            const L = (back * 10) + front; 
            // 결과: 2341
            ```
            
        - 오른쪽 회전 (R): `1234` ➔ `4123`
            
            ```jsx
            // n = 1234 라고 가정
            
            // [1단계] 맨 뒷자리(일의 자리) 구하기
            const back = n % 10; // 4
            
            // [2단계] 나머지 앞부분 구하기
            const front = Math.floor(n / 10); // 123
            
            // [3단계] 합치기
            // 뒷자리(4)를 천의 자리로 보내고(* 1000) + 앞부분(123)을 뒤에 붙임
            const R = (back * 1000) + front; 
            // 결과: 4123
            ```
            
    3. 숫자 순환 (범위 넘어가면 0으로)
        
        > 시계처럼 숫자가 뱅글뱅글 도는 연산입니다.
        > 
        - 증가 후 순환: 숫자가 `9999`를 넘어가면 `0`이 되게 하고 싶을 때.
            
            ```jsx
            // 공식: (n + 1) % MOD
            
            const MOD = 10000;
            
            // 9999에서 1을 더하면?
            const D = (9999 + 1) % MOD; 
            // 결과: 10000 % 10000 = 0 (나머지만 남음)
            ```
            
            ```jsx
            const N = 5; // 배열 길이
            let idx = 0;
            
            // 다음 칸으로 이동 (4 -> 0으로 순환)
            idx = (idx + 1) % N; 
            
            // 3칸 점프 (범위 벗어나도 안전)
            idx = (idx + 3) % N;
            ```
            
        - 감소 후 순환: 숫자가 `0`에서 `1`을 뺐을 때 `9999`가 되게 하고 싶을 때.
            
            ```jsx
            // 공식: (n - 1 + MOD) % MOD
            
            const MOD = 10000;
            let n = 0;
            
            // [나쁜 예] 그냥 빼기
            // (0 - 1) % 10000 -> -1 (실패)
            
            // [좋은 예] 더하고 나누기
            const S = (n - 1 + MOD) % MOD;
            // 계산: (0 - 1 + 10000) % 10000 
            //      = 9999 % 10000 
            //      = 9999 (성공!)
            ```
            
    4. 큰 수 연산 (Overflow 방지)
        
        > JS의 `Number`는 $2^{53}-1$을 넘어가면 값이 깨진다. 중간중간 잘라줘야 한다.
        >
