[https://www.acmicpc.net/problem/14502](https://www.acmicpc.net/problem/14502)
```javascript
const input = require('fs')
    .readFileSync(process.platform === 'linux' ? '/dev/stdin' : './input.txt')
    .toString()
    .trim()
    .split('\n');

let inputIndex = 1;

// N: 세로, M: 가로
const [N, M] = input[0].split(' ').map(Number);

// 안전 영역 크기
let answer = 0;

// 지도 만들기
const maps = [];
for (let i = 1; i < input.length; i++) {
    maps.push(input[inputIndex++].split(' ').map(Number));
}

// 빈칸 캐싱 하기
const emptySpaces = [];
for (let y = 0; y < N; y++) {
    for (let x = 0; x < M; x++) {
        if (maps[y][x] === 0) {
            emptySpaces.push([y, x]);
        }
    }
}

const dy = [-1, 0, 1, 0];
const dx = [0, 1, 0, -1];

// 바이러스 시뮬레이션 함수
function virusBFS() {
    const queue = [];

    // 바이러스 시뮬레이션할 때 원본 maps를 건드리면 안된다. 깊은 복사를 해야한다.
    const replicaMaps = maps.map((row) => [...row]);

    // 초기값 넣기
    for (let y = 0; y < N; y++) {
        for (let x = 0; x < M; x++) {
            if (replicaMaps[y][x] === 2) {
                queue.push([y, x]);
            }
        }
    }
    // 바이러스 전파 시작
    while (queue.length > 0) {
        // 현재 위치
        const [curY, curX] = queue.shift();

        // 상하좌우 확인
        for (let i = 0; i < 4; i++) {
            const ny = curY + dy[i];
            const nx = curX + dx[i];

            // 전염되는 조건1) 유효한 범위 조건2) 값이 0인 곳
            if (ny >= 0 && nx >= 0 && ny < N && nx < M && replicaMaps[ny][nx] === 0) {
                // 전염
                replicaMaps[ny][nx] = 2;

                // 큐에 넣음
                queue.push([ny, nx]);
            }
        }
    }

    // 전염 끝.
    let safeArea = 0;

    // 안전 영역 계산
    for (let y = 0; y < N; y++) {
        for (let x = 0; x < M; x++) {
            if (replicaMaps[y][x] === 0) {
                safeArea++;
            }
        }
    }

    // 안전 영역 크기 비교
    answer = Math.max(answer, safeArea);
}

// 백트래킹 함수
function backTracking(wallCount, startIndex) {
    // 기저 조건: 벽이 3개 일 때
    if (wallCount === 3) {
        // BFS
        virusBFS();

        // 바이러스 시뮬레이션 끝나면 복귀
        return;
    }

    // 기본 조건: maps를 순회하며 0인 곳에 벽을 세운다.
    // 이때, 순서는 중요하지 않으니 조합으로 구현한다.
    for (let i = startIndex; i < emptySpaces.length; i++) {
        // 캐싱한 빈칸 좌표 꺼내기
        const [y, x] = emptySpaces[i];

        // 벽 세우기
        maps[y][x] = 1;

        // 재귀
        backTracking(wallCount + 1, i + 1); // 다음 건너뛴다.

        // 나오면 벽 허물기
        maps[y][x] = 0;
    }
}

// 0개의 벽으로 시작
backTracking(0, 0);

console.log(answer);
```
### 🔗 풀이
1. 준비물
    - 빈칸 캐싱 배열
2. 백트래킹으로 3개의 벽을 세운다.
    - 벽을 세운다.
    - 재귀한다.
    - 재귀에서 나오고 벽을 허문다.
3. 벽이 3개 세워졌으면 바이러스가 얼마나 퍼지는지 시뮬레이션한다.
    - 이떄 “벽이 다 세워진” 타이밍은, 백트래킹의 기저 조건에 도달했을 때이다.
4. BFS (시뮬레이션)
    - 시뮬레이션에 사용될 `maps` 복사본을 준비한다. (깊은)
    - 큐에 넣고 전파 시작
    - 안전 영역을 센다.

### 🔗 배운점
1. 벽을 세우는 기준?
    - 그런건 없다. 지도의 최대 크기는 64이고, 이중 바이러스와 벽의 기본값을 빼면
        
        ($_{60}C_3$)
      $\frac{60 \times 59 \times 58}{3 \times 2 \times 1} = 34,220$
       으로 경우의 수가 별로 안된다. 브루투스 알고리즘으로 전부 해보자.
        
2. 바이러스를 퍼뜨리는 타이밍 === 백트래킹의 기저 조건에 도달했을 때 === 깊이가 3이 되었을 때.
3. 시간복잡도가 괜찮은 이유
    
    > 조합 $O(N^3)$ 안에 BFS $O(V+E)$
    > 
    1. 벽을 세우는 경우의 수 (조합)
        - 약 35,000번
    2. 바이러스가 퍼지는 시뮬레이션 (BFS)
        - 노드(칸) 개수: 최대 **64개**
        - 간선(상하좌우 이동): 최대 $64 \times 4 = 256$개 (간선이 상하좌우 고정이라 4만 곱하면 된다.)
        - BFS 1회 연산량: $O(V+E) \approx 64 + 256 \approx 300$번 (상수 시간) $\text{총 연산량} \approx 35,000 \times 300 = 10,500,000 \text{ (약 1,000만 번)}$
4. 백트래킹에서 방문 여부 배열이 필요없는 이유
    - `maps`를 한번씩 순회하기 때문이다.
    - 또한, 지도(`maps`)에 직접 1을 쓰고 다시 0으로 지우는 행위 자체가 방문 처리를 대신하고 있ek.
5. `virusBFS()` 에서 새로운 `maps`를 만들어야 하는 이유
    - 벽을 세울 때마다 시뮬레이션을 돌려야 하기 때문에 원본 배열은 남겨놔야 한다.
6. 깊은 복사하기
    
    ```jsx
    
    // ✅ 각 행(row)마다 새로운 배열([...row])을 만들어서 반환
    const replicaMaps = maps.map(row => [...row]);
    
    // ❌ 참조만 복사됨. 둘은 한 몸임.
    const replica = maps; 
    
     // ❌ 겉껍데기(행)만 새것이고, 속(열)은 공유됨.
    const replica = [...maps];
    ```
    
7. 최적화) ⭐️ 빈칸 좌표 캐싱하기
    
    > 벽은 빈칸에만 세울 수 있다. 미리 빈칸을 파악하면 나중에 유효한 빈칸만 확인하면 된다.
    > 
8. 최적화) 조합으로 구현하기
    
    > 벽 세울 때 순서는 중요하지 않다.
    > 
9. 최적화) 큐 `shift()`
    
    ```jsx
      let head = 0;
      while (queue.length > head) {
          const [y,x] = queue[head++];
    ```
    
    - `index`변수를 만들어 놓으면 큐를 굳이 안 만들어도 최적화가 가능하다.
