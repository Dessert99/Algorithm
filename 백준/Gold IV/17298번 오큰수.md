[https://www.acmicpc.net/problem/17298](https://www.acmicpc.net/problem/17298)
```javascript
const input = require('fs')
    .readFileSync(process.platform === 'linux' ? '/dev/stdin' : './input.txt')
    .toString()
    .trim()
    .split('\n');

let inputIndex = 0;

const N = Number(input[inputIndex++]);

const nums = input[inputIndex].split(' ').map(Number);

// 원소의 인덱스를 넣는다.
const stack = [];

const answer = Array.from({ length: N }, () => -1);

for (let i = 0; i < N; i++) {
    // 지금 확인하는 수
    const curNum = nums[i];

    // 스택 꼭대기부터 현재 수와 비교한다. 현재 수가 더 크면 뺀다.
    while (stack.length > 0 && nums[stack[stack.length - 1]] < curNum) {
        // 오큰수를 찾았으니, 스택에서 해당 번호 인덱스를 꺼낸다.
        const topIndex = stack.pop();
        //
        answer[topIndex] = curNum;
    }

    // 현재 인덱스도 스택에 넣는다.
    stack.push(i);
}

console.log(answer.join(' '));
```
### 🔗 풀이

### 🔗 배운점
1. `stack`의 역할
   - 아직 오큰수를 만나지 못한 원소들이 들어가있다.
   - 순회다가 오큰수를 만나게 되면 `stack`에서 빠져나와 `answer`배열에 기록한다.
2. 순서가 유지되는 이유
   - 스택에 넣기 전에, 마지막 요소가 넣으려는 숫자보다 큰지 확인하고 넣는다.
