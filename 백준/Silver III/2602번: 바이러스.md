[https://www.acmicpc.net/problem/2606](https://www.acmicpc.net/problem/2606)

```javascript
const input = require('fs')
    .readFileSync(process.platform === 'linux' ? '/dev/stdin' : './input.txt')
    .toString()
    .trim()
    .split('\n');

// 배열 인덱스
let index = 0;

// 컴퓨터 개수
const N = Number(input[index++]);

// 연결된 쌍 개수
const couples = Number(input[index++]);

// 현재 네트워크 상태 만들기
const network = Array.from({ length: N }, () => Array(N).fill(false));
for (let i = 0; i < couples; i++) {
    const [st, en] = input[index + i].split(' ').map(Number);

    // 이때 방향이 없기 때문에 양쪽 모두 표시해야 한다.
    network[st - 1][en - 1] = true;
    network[en - 1][st - 1] = true;
}

let count = 0; // 추가로 바이러스에 걸린 컴퓨터 개수.
const queue = [];

// 컴퓨터 방문 여부 배열
const visited = Array.from({ length: N }, () => false);

queue.push(0); // 1번 컴퓨터부터 시작
visited[0] = true; // 1번 컴퓨터 방문

while (queue.length > 0) {
    const computer = queue.shift(); // 현재 방문한 컴퓨터

    // 전파 가능성 확인
    for (let j = 0; j < N; j++) {
        // 1) 현재 컴퓨터와 연결되어 있거나 2) 아직 방문하지 않은 컴퓨터라면 전파
        if (network[computer][j] === true && visited[j] === false) {
            queue.push(j);
            count++;
            visited[j] = true; // 방문 표시
        }
    }
}

console.log(count);
```
### 🔗 배운점
1. 변수명
   - `computer` -> `curr`
2. 1차원 배열을 초기화
   - 수정 전: `const visited = Array.from({ length: N }, () => false);`
   - 수정 후: `const visited = new Array(N).fill(false);`
3. 시간복잡도 계산
   - 현재 내 코드(인접 행렬) : 모든 컴퓨터($N$)를 순회하며, 각 컴퓨터에 연결 여부를 확인($N$)한다. -> $O(N^2)$
   - 컴퓨터는 100 이하 정수이다. -> $100^2 = 10,000$
