[https://www.acmicpc.net/problem/2579](https://www.acmicpc.net/problem/2579)
```javascript
const input = require('fs')
    .readFileSync(process.platform === 'linux' ? '/dev/stdin' : './input.txt')
    .toString()
    .trim()
    .split('\n');

let inputIndex = 0;

const N = Number(input[inputIndex++]);

const stairs = [0];
for (let i = 0; i < N; i++) {
    stairs.push(Number(input[inputIndex++]));
}

// dp[i]가 의미하는 것은 "i번째 계단까지 왔을 때 최댓값"이다.
const dp = Array.from({ length: N + 1 }, () => 0);

// 초기값 설정: 우리는 최대 2칸 전까지 확인한다.
dp[1] = stairs[1];
if (N >= 2) {
    dp[2] = stairs[1] + stairs[2];
}

// 순회
for (let i = 3; i <= N; i++) {
    // 2칸 전에서 온 경우: i-2까지의 최댓값 + 현재 계단 점수
    const before2 = dp[i - 2] + stairs[i];

    // 1칸 전에서 온 경우: i-3까지의 최댓값 + 직전 계단 점수 + 현재 계단 점수
    const before1 = dp[i - 3] + stairs[i - 1] + stairs[i];

    // 더 큰 값이 i번째 계단까지 왔을 때 최댓값이다.
    dp[i] = Math.max(before1, before2);
}
console.log(dp[N]);
```
### 🔗 풀이
1. 💡 조건 분석
    1. 마지막 계단은 무조건 밟아야 한다. -> 뒤에서부터 체크해야 하는가? -> ❌
    2. 연속된 세 개의 계단을 모두 밟아서는 안 된다. ->  2칸 2칸 2칸.. 이런식은 되느넫, 1칸 1칸 1칸 이렇게는 안되는 듯?

2. 🤔 시뮬레이션?
    - 경우의 수가 너무 많다. 일일히 계단을 오르는 모든 상황을 고려하지는 못할듯? -> dp문제이다.

3. 🤔 dp[i]는 무엇을 의미할까?
    - dp[i]까지 왔을 때 최댓값.

4. 🤔 초기값을 설정하기 위해 현재 위치에서 비교할 것들이 무엇인지 생각해보자.
    1. dp[i]이전 dp[i-1], dp[i-2] 값은 완벽하게 구해져 있다.
    2. i번째 위치까지 갔을 때, 이전 위치는 바로 전 칸에서 왔거나, 2칸 전에서 왔다.
    3. 전 칸에서 왔다면? -> dp[i-3] -> stairs[i-1] + stairs[i] 순서가 분명하다.
    4. 2칸 전에서 왔다면? -> i-2까지 최댓값 + 현재 계단 점수
        - 🤔 왜??
            - i-2에서 왔다면, 지금 위치까지 2칸 점프했다. 3칸 연속 조건에 종속되지 않아서 현재 계단 점수를 확실하게 추가할 수 있다.
              
5. 🤔 dp[i]와 stairs[i]의 차이점은?
    - dp[i]: 여기까지 오면서 얻은 최대 누적 점수
    - stairs[i]: 이 계단의 점수
    - ⭐️ dp[i]는 결국, 과거의 모든 최선의 선택 + stairs[i]이다.

### 🔗 배운점
1. dp적 사고력으로 문제 풀기    
    - 조건: 3계단 연속 밟기 금지 → 마지막 한 걸음만 보고 식을 세우기
    - 현재 위치에서 확인할 것은, 바로 직전에서 왔는가?, 2칸 전에서 왔는가? 이다.
    - 만약 2칸 전에서 왔다면 -> 3칸 연속 조건에서 자유롭다. 연속된 계단은 여기 뿐!
    - 만약 1칸 전에서 왔다면 -> 만약 1칸 전에서도 1칸만 왔다면, 지금 내 위치는 연속적인 3번째 칸이다. 즉, 1칸 전에는 2칸 점프를 했다. -> 이전 단계는 완벽하다고 가정한다. = dp적 사고
2. ⭐️ dp를 풀 때 시뮬레이션을 돌리지 말자.
    - dp적 사고: 결과 중심
        - 🙋🏻‍♂️ 나는 지금 i번째 계단에 도착했어. 내가 어떻게 왔는지는 알 바 아니야. 단지, 내 바로 전 위치가 어디였는지만 중요해
    - 귀납적 사고력 기르기
        - 이전 단계($i-1, i-2...$)까지의 정답(dp값)은 이미 완벽하게 구해져 있다고 믿는다.

