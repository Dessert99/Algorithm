[https://www.acmicpc.net/problem/2559](https://www.acmicpc.net/problem/2559)
```javascript
const input = require('fs')
    .readFileSync(process.platform === 'linux' ? '/dev/stdin' : './input.txt')
    .toString()
    .trim()
    .split('\n');

let inputIndex = 0;

// N: 날짜 수(2~10만), K: 연속적인 날짜 수(1~N)
const [N, K] = input[inputIndex++].split(' ').map(Number);

// 온도 배열
const temperature = input[inputIndex].split(' ').map(Number);

// 누적합 배열
const S = Array.from({ length: N + 1 }, () => 0);

for (let i = 1; i <= N; i++) {
    S[i] = S[i - 1] + temperature[i - 1];
}

// 연속적인 K일 간의 온도의 합
const sumTemperature = [];

for (let i = K; i <= N; i++) {
    sumTemperature.push(S[i] - S[i - K]);
}

console.log(Math.max(...sumTemperature));
```
### 🔗 풀이
1. 🤔 slice를 사용한다면?
    - 배열을 순회하며 매번 slice를 사용한다면 메모리 초과 위험이 있을 것 같다.
    - slice는 항상 새로운 배열을 만들어 반환하기 떄문이다.
### 🔗 배운점
1. 누적합 템플릿
   ```javascript
      // 1. 입력 (원본 배열 A)
      const A = [1, 3, 10, 6, 8, 12, 7];
      const N = A.length;
      
      // 2. 누적 합 배열 S 생성 (크기 N+1, 0으로 초기화)
      // S[i]는 A[0]부터 A[i-1]까지의 합
      const S = new Array(N + 1).fill(0);
      
      // Build: 누적 합 구하기
      for (let i = 1; i <= N; i++) {
        S[i] = S[i - 1] + A[i - 1];
      }
      
      console.log("S 배열:", S);
      // 출력: [ 0, 1, 4, 14, 20, 28, 40, 47 ]
      
      // 3. 구간 합 구하기 (Query)
      // 예: 인덱스 2부터 4까지의 합 (10 + 6 + 8 = 24)
      const start = 2;
      const end = 4;
      
      // 공식: S[end + 1] - S[start]
      const sum = S[end + 1] - S[start];
      
      console.log(`구간 합 (${start}~${end}):`, sum); // 24
   ```
   - 공식: `S[i] = S[i - 1] + A[i - 1] (이전까지의 누적합 + 이번에 추가할 원본 값)`
2. 과정
   - 원본 배열 `A: [1, 3, 10, 6, 8, 12, 7]`  
        | **반복 i** | **A[i - 1](새로 더할 값)** | **S[i - 1](직전까지의 합)** | **S[i] 계산(새로운 누적합)** | **실시간 S 배열 상태** |
        | --- | --- | --- | --- | --- |
        | **초기** | - | - | - | `[0, 0, 0, 0, 0, 0, 0, 0]` |
        | **1** | `A[0]` = **1** | `S[0]` = 0 | 0 + 1 = **1** | `[0, 1, 0, 0, 0, 0, 0, 0]` |
        | **2** | `A[1]` = **3** | `S[1]` = 1 | 1 + 3 = **4** | `[0, 1, 4, 0, 0, 0, 0, 0]` |
        | **3** | `A[2]` = **10** | `S[2]` = 4 | 4 + 10 = **14** | `[0, 1, 4, 14, 0, 0, 0, 0]` |
        | **4** | `A[3]` = **6** | `S[3]` = 14 | 14 + 6 = **20** | `[0, 1, 4, 14, 20, 0, 0, 0]` |
        | **5** | `A[4]` = **8** | `S[4]` = 20 | 20 + 8 = **28** | `[0, 1, 4, 14, 20, 28, 0, 0]` |
        | **6** | `A[5]` = **12** | `S[5]` = 28 | 28 + 12 = **40** | `[0, 1, 4, 14, 20, 28, 40, 0]` |
        | **7** | `A[6]` = **7** | `S[6]` = 40 | 40 + 7 = **47** | `[0, 1, 4, 14, 20, 28, 40, 47]` |
3. 2차원 누적합 스니펫
   > **큰 거** (`S[끝][끝]`)에서  
    **위**(`S[시작-1][끝]`) 빼고  
    **왼**(`S[끝][시작-1]`) 빼고  
    **대각선 위**(`S[시작-1][시작-1]`) 더한다.
    >
   ```javascript
        // 1. 입력 (원본 2차원 배열 A, 크기 N x M)
        const A = [
          [1, 2, 3],
          [4, 5, 6],
          [7, 8, 9]
        ];
        const N = A.length; // 행 (y축 길이)
        const M = A[0].length; // 열 (x축 길이)
        
        // 2. 누적 합 배열 S 생성 (크기 (N+1) x (M+1), 0으로 초기화)
        // S[y][x]는 (0,0)부터 (y-1, x-1)까지 직사각형 구간의 총합
        const S = Array.from(Array(N + 1), () => new Array(M + 1).fill(0));
        
        // Build: 2차원 누적 합 구하기
        for (let y = 1; y <= N; y++) {
          for (let x = 1; x <= M; x++) {
            // 공식: 위쪽 합 + 왼쪽 합 - 중복으로 더해진 대각선 위쪽 합 + 현재 원소
            S[y][x] = S[y - 1][x] + S[y][x - 1] - S[y - 1][x - 1] + A[y - 1][x - 1];
          }
        }
        
        console.log("S 배열 (누적합 테이블):");
        console.table(S);
        
        // 3. 2차원 구간 합 구하기 (Query)
        // 예: 원본 배열 기준 (1,1)부터 (2,2)까지의 합 (5 + 6 + 8 + 9 = 28)
        // y는 행(Row), x는 열(Column)을 의미합니다.
        const y1 = 1, x1 = 1;
        const y2 = 2, x2 = 2;
        
        // 공식: 목표 큰 사각형 - 잘라낼 위쪽 사각형 - 잘라낼 왼쪽 사각형 + 중복으로 잘려나간 부분 복구
        const sum = S[y2 + 1][x2 + 1] - S[y1][x2 + 1] - S[y2 + 1][x1] + S[y1][x1];
        
        console.log(`구간 합 ((y:${y1}, x:${x1}) ~ (y:${y2}, x:${x2})):`, sum); // 28
   ```
