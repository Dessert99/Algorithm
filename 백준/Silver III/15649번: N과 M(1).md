[https://www.acmicpc.net/problem/15649](https://www.acmicpc.net/problem/15649)
```javascript
const input = require('fs')
    .readFileSync(process.platform === 'linux' ? '/dev/stdin' : './input.txt')
    .toString()
    .trim()
    .split('\n');

// N: 1~N 까지 ,  M: 수열 길이
const [N, M] = input[0].split(' ').map(Number);

// 방문 여부 배열
const visited = Array.from({ length: N }, () => false);

// 재사용할 배열
const tmp = [];

// 정답 배열
const answer = [];

// 백트래킹
function backTracking(length) {
    // 기저 조건: 배열 길이가 M이 되었을 때
    if (length === M) {
        // 수열 출력
        answer.push(tmp.join(' '));
        return;
    }

    // 기본 조건: 재귀
    for (let i = 0; i < N; i++) {
        // 아직 tmp배열에 넣지 않은 요소만
        if (visited[i] === false) {
            tmp.push(i + 1);
            visited[i] = true;

            // 재귀
            backTracking(length + 1);

            // 재귀 나오면 배열에서 제거
            tmp.pop();
            visited[i] = false;
        }
    }
}

backTracking(0);

console.log(answer.join('\n'));
```
### 🔗 풀이
1. 수열을 만들 때 사용할 `tmp`배열을 만든다. 이 배열에 요소를 넣고 빼며 모든 수열을 만든다.
2. 중복 방문 방지를 위해 `visited` 배열을 만든다. 이미 `tmp`배열에 들어간 요소는 탐색하지 않는다.
3. `backtracking` DFS함수를 구현한다. 재귀 깊이가 `M`에 도달하는 시점을 기저 조건으로 설정한다. 이때 완성된 순열을 결과 배열에 저장한다.
4. `backtracking` 기본 로직은 1부터 `N`까지 순회하며, 아직 방문하지 않은 요소들만 재귀 호출하여 탐색한다.

### 🔗 배운점
1. `console.log` 은 꼭 한 번 만 사용하자.
