[https://www.acmicpc.net/problem/1202](https://www.acmicpc.net/problem/1202)
```javascript
const input = require('fs')
    .readFileSync(process.platform === 'linux' ? '/dev/stdin' : './input.txt')
    .toString()
    .trim()
    .split('\n');

//우선 순위 큐
class MaxHeap {
    constructor() {
        this.heap = [];
    }

    swap(i1, i2) {
        [this.heap[i1], this.heap[i2]] = [this.heap[i2], this.heap[i1]];
    }
    getParentIndex(curIndex) {
        return Math.floor((curIndex - 1) / 2);
    }
    getLeftChildIndex(curIndex) {
        return curIndex * 2 + 1;
    }
    getRightChildIndex(curIndex) {
        return curIndex * 2 + 2;
    }

    insert(value) {
        this.heap.push(value);
        this.heapifyUp();
    }
    heapifyUp() {
        // 마지막 인덱스
        let curIndex = this.heap.length - 1;

        // 부모와 비교 시작
        while (curIndex > 0) {
            const parentIndex = this.getParentIndex(curIndex);

            // 올라가는 조건? -> 내가 부모보다 클 때. 그러니까, 종료 조건은 부모가 나보다 클 때.
            if (this.heap[parentIndex] >= this.heap[curIndex]) {
                break;
            }

            // 올라간다.
            this.swap(curIndex, parentIndex);
            curIndex = parentIndex;
        }
    }

    extractMax() {
        if (this.heap.length === 0) {
            return null;
        }
        if (this.heap.length === 1) {
            return this.heap.pop();
        }

        const max = this.heap[0];

        this.heap[0] = this.heap.pop();

        this.heapifyDown();

        return max;
    }

    heapifyDown() {
        // 루트에 있는 요소를 아래로 내린다.
        let curIndex = 0;

        const n = this.heap.length;

        while (true) {
            let maxIndex = curIndex;
            const leftChildIndex = this.getLeftChildIndex(curIndex);
            const rightChildIndex = this.getRightChildIndex(curIndex);

            // 왼쪽 자식부터 비교. 이때, 자식 인덱스가 유효한지 힙의 길이와 비교해야 한다.
            // 지금 요소가 자식보다 작을 경우 -> 내려간다.
            if (leftChildIndex < n && this.heap[leftChildIndex] > this.heap[maxIndex]) {
                maxIndex = leftChildIndex;
            }

            if (rightChildIndex < n && this.heap[rightChildIndex] > this.heap[maxIndex]) {
                maxIndex = rightChildIndex;
            }

            if (maxIndex === curIndex) {
                break;
            }

            this.swap(maxIndex, curIndex);
            curIndex = maxIndex;
        }
    }
}

let inputIndex = 0;

// N: 보석 개수(1~30만), K: 가방 개수(1~30만)
const [N, K] = input[inputIndex++].split(' ').map(Number);

// 보석 정보
const jewelInfo = [];
for (let i = 0; i < N; i++) {
    // [M, V]: 보석 무게, 보석 가격 -> 둘 다 최대 백만
    jewelInfo.push(input[inputIndex++].split(' ').map(Number));
}

// 가방에 담을 수 있는 무게
const bags = [];
for (let i = 0; i < K; i++) {
    // C: 가방에 담을 수 있는 최대 무게 (최대 1억)
    bags.push(Number(input[inputIndex++]));
}

// 가방과 보석을 무게가 작은 순으로 정렬한다.
bags.sort((a, b) => a - b);
jewelInfo.sort((a, b) => a[0] - b[0]);

const pq = new MaxHeap();

let answer = 0;

let head = 0;

// 가방 순회
for (const bag of bags) {
    // 이 가방에 넣을 수 있는 모든 보석을 우선 순위 큐에 넣는다. M: 무게 V:가치
    while (jewelInfo.length > head) {
        // 현재 보석
        const [curM, curV] = jewelInfo[head];

        if (bag < curM) {
            break;
        }

        if (bag >= curM) {
            pq.insert(curV);
            head++;
        }
    }

    // 우선 순위 큐 루트에 있는 요소는 가치가 가장 큰 보석이다.
    const maxValue = pq.extractMax();
    answer += maxValue;
}
console.log(answer);
```
### 🔗 풀이
1. 💡 시뮬레이션 해보기
    1. jewelInfo = [ [5, 10], [100, 100] ]
        bags = [ 11 ]
    2. 가방의 한도는 11이고, 보석의 무게는 5, 100 두가지이다. 100은 11에 넣지 못하니, 무게가 5인 보석을 넣을 수 있고, 답은 10이다.

    1. jewelInfo = [ [ 1, 65 ], [ 5, 23 ], [ 2, 99 ] ]
        bags = [ 10, 2 ]
    2. 첫 번째 보석과 세 번째 보석을 각 가방에 넣으면 된다. 왜? -> 💡 가방에 최대 한 개의 보석을 넣을 수 있다.
    3. 여기서 무게가 5인 보석을 첫 번째 가방에 넣으면 안 된다. -> 넣었다가 빼면 되지 않을까?

2. 🤔 용량이 작은 가방부터 채워야 할까, 큰 가방부터 채워야 할까?
    - 큰 가방 부터 채워야 한다. -> 보석과 가방을 모두 무게 기준 내림차순 정렬한다. -> ❌ 작은 가방부터 담아야 한다. 왜? 가방 하나에 최대 한 개의 보석을 담을 수 있기 때문이다. 작은 보석은 선택지가 많다.
### 🔗 배운점
1. ⭐️ 왜 우선 순위 큐일까?
    - 보석의 가치를 매번 정렬하면 보석은 최대 30만 개이기 때문에 시간 초과가 발생한다.
2. 발상의 전환
    > "이 보석을 어떤 가방에 넣을지?"가 아니라 "이 가방에 어떤 보석을 담을지?"를 고려해보자.
    1. 가방을 중점으로 두고 반복문
    2. 작은 가방에서 담을 수 있는 보석 확인 후 우선 순위 큐에 넣음
    3. 이떄 최대 힙으로 만든다.
    4. 다음 가방을 꺼낸다. 이때, ⭐️ 우리는 작은 가방부터 확인했기 때문에 이미 우선 순위 큐에 있는 보석들은 이 가방에 담을 수 있다.
    5. 새로운 가방에 넣을 수 있는 보석들을 우선 순위 큐에 넣는다.
    6. 반복한다.
3. 논리적 사고 과정
   - 가방과 보석의 최대 크기가 각각 30만이다. -> 순차 탐색, 배열을 만들기(dp) 조건에 맞지 않는다. -> 효율적인 알고리즘 필요함. -> 우선 순위 큐?
   - 가방과 보석 중 무엇을 먼저 처리해야 할까? -> "선택지가 적은 작은 가방부터 처리하자" -> 가방을 오름차순으로 정리해야 한다.
   - 가방과 보석, 둘 중 무엇이 우선인가? -> 보석은 여러 가방에 넣을 수 있지만 가방은 하나의 보석만 넣을 수 있다. -> 작은 가방부터 처리한다.
