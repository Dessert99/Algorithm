[https://www.acmicpc.net/problem/1541](https://www.acmicpc.net/problem/1541)
```javascript
const input = require('fs')
    .readFileSync(process.platform === 'linux' ? '/dev/stdin' : './input.txt')
    .toString()
    .trim()
    .split('\n');

let inputIndex = 0;

const arr = input[inputIndex].split('-');

let min = 0;

// 첫 번째 묶음
const first = arr[0].split('+').map(Number);

for (let i = 0; i < first.length; i++) {
    min += first[i];
}

// 두 번째 묶음 부터는 음수이다.
for (let i = 1; i < arr.length; i++) {
    const cur = arr[i].split('+').map(Number);

    for (let j = 0; j < cur.length; j++) {
        min -= cur[j];
    }
}
console.log(min);
```
### 🔗 풀이
1. 💡 우리는 괄호를 쳐야 한다.

2. 🤔 어디에 괄호를 쳐야 최솟값이 될까?
    - 일단 핵심은 - 이다.

3. 🤔 일단 문자열을 파싱해서 식을 완성해야 하는가?
    - 아닌 것 같다.
4. 🤔 '-' 기준으로 split한다면
    - 첫 번째 묶음은 무조건 양수이다.
    - 두 번째 묶음부터는 음수이며, 괄호로 묶인 숫자들이다. 이들은 모두 음수로 취급된다.
    - 이제 각 묶음을 파싱하여 계산한다.

### 🔗 배운점
1. 핵심 파악하기: '-'에 주목하자
   - 최솟값이 되려면 음수가 많아야 한다.
   - 분배 법칙에 의해, 괄호 앞 -는 모든 요소의 부호를 변경한다.
   - 우리는 "언제" -가 나오는지 파악하고, 그 부분에만 집중하면 된다.
