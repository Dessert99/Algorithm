[https://school.programmers.co.kr/learn/courses/30/lessons/43162?language=javascript](https://school.programmers.co.kr/learn/courses/30/lessons/43162?language=javascript)
```javascript
function solution(n, computers) {
    var answer = 0;

    // 방문 여부 배열
    const visited = Array.from({ length: n }, () => false);

    // 큐
    class Queue {
        constructor() {
            this.store = [];
            this.front = 0;
            this.rear = 0;
        }
        size() {
            return this.rear - this.front;
        }
        enqueue(value) {
            this.store[this.rear] = value;
            this.rear++;
        }
        dequeue() {
            if (this.front === this.rear) {
                return undefined;
            }
            const value = this.store[this.front];
            this.front++;
            return value;
        }
    }

    // n개의 노드 확인
    for (let i = 0; i < n; i++) {
        // 방문한 적이 없는 노드일 때만 시작
        if (visited[i] === false) {
            // 큐 생성
            const q = new Queue();

            // 현재 노드를 큐에 넣는다.
            q.enqueue(i);

            // 방문 표시
            visited[i] = true;

            // 네트워크 확인
            while (q.size() > 0) {
                const curNode = q.dequeue();
                // 연결된 노드 확인
                for (let j = 0; j < n; j++) {
                    // 조건1) 연결되어 있다 조건2) 아직 방문하지 않았다.
                    if (computers[curNode][j] === 1 && visited[j] === false) {
                        //연결되어 있으니 큐에 넣음
                        q.enqueue(j);
                        //방문 표시
                        visited[j] = true;
                    }
                }
            }
            // 네트워크 확인 종료
            answer++;
        }
    }

    return answer;
}
```
### 🔗 배운점
1. 시간복잡도 계산
   - 이런 문제는 인접 행렬 방식이다. 현재 노드와 연결된 노드를 찾기 위해서는 `computers` 배열의 모든 요소를 확인해야 한다. 최대 `n`값은 200이므로, $O(V^2)$ = 40,000번의 연산이 필요하다.
   - 참고로 인접 리스트 방식 문제는 연결된 노드만 리스트에 담겨 있으므로 (`graph = [[1, 2], [0], [0]]`) 모든 노드($V$)를 방문하고, 간선 개수($E$)만큼만 확인하기 때문에 *$O(V + E)$이다.
2. BFS나 DFS의 효율성
   - 나의 걱정: `for`문이 `$N%`번 돌고, 각 루프에서 BFS가 실행되고, 그 안에서 $N$번(혹은 $N^2$번) 연산하기 떄문에 $O(N^3)$의 시간복잡도가 되는게 아닌지 걱정
   - 실제 작동:
     ```javascript
        // [외부 루프] : 0부터 N-1까지 (총 N회 시도)
          for (let i = 0; i < n; i++) {
              // 여기서 검문
              if (visited[i] === false) { 
                  // BFS는 여기서 '딱 한 번' 시작됨
                  BFS(i); 
              }
          }
     ```
     방문 여부 배열로 한 번 확인하기 때문에 모든 노드는 딱 한 번만 BFS에 들어간다.
     
