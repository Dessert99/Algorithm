[https://school.programmers.co.kr/learn/courses/30/lessons/151139?language=mysql](https://school.programmers.co.kr/learn/courses/30/lessons/151139?language=mysql)
```sql
SELECT MONTH(START_DATE) AS MONTH, CAR_ID, COUNT(*) AS RECORDS
    
FROM CAR_RENTAL_COMPANY_RENTAL_HISTORY

-- 이게 서브쿼리에서 가져오는 방법이다. CAR_ID로 정보를 매핑한다고 생각.
WHERE CAR_ID IN
    (SELECT CAR_ID -- 외부 쿼리와 매핑할 수 있다. COUNT는 넘겨주지 않는다. 아래 HAVING에서 5개 조건을 고려했다.
    FROM CAR_RENTAL_COMPANY_RENTAL_HISTORY
    WHERE MONTH(START_DATE) >= 8 AND MONTH(START_DATE) <=10
    GROUP BY CAR_ID
    HAVING COUNT(START_DATE) >= 5 -- 그룹화 덩어리들마다 조건을 걸 때는 HAVING
    ) AND 
    MONTH(START_DATE) >= 8 AND MONTH(START_DATE) <=10 -- 외부 쿼리에도 조건 필요
      
-- 월별 그룹
GROUP BY MONTH(START_DATE), CAR_ID

ORDER BY MONTH(START_DATE) , CAR_ID DESC

```
### 🔗 풀이
1. 🤔 2022년 8월부터 2022년 10월까지 총 대여 횟수가 5회 이상인 자동차들: 서브쿼리
   1. WHERE절
        - START_DATE가 5개 이상인 CAR_ID만 가져온다.
    2. HAVING
        - 5
   
2. 🤔 해당 기간 동안의 월별 자동차 ID 별 총 대여 횟수(컬럼명: RECORDS) 리스트를 출력
    - GROUP BY + 집계

3. 🤔 MONTH 표현하기
    1. MONTH 문법 사용하기
        - 자동으로 월만 출력된다.

4. 🤔 특정 월의 총 대여 횟수가 0인 경우에는 결과에서 제외
    - GROUP BY에서 자동으로 해준다.
### 🔗 배운점
1. DATE타입을 그룹화할 때 주의점
    - 월별로 그룹화할 때는 START_DATE자체를 넣는 것이 아니라 MONTH()로 묶어야 한다.
    - ORDER BY할 때도 주의해야 한다. 분, 초까지 있기 때문에 같은 데이터로 취급되지 않는다.
2. CAR_ID별로 5개 이상인지 파악하는 문장은 그룹화 이후에 필터링을 해야 한다.
   - 이럴 때는 HAVING을 사용해야 한다.
3. WHERE절 서브쿼리
   - 서브쿼리에서 HAVING으로 5개 이상인 행을 필터링했다. 이제 외부 쿼리로 꺼내려면 CAR_ID컬럼으로 매핑해야 한다.
   - ⭐️⭐️ 이때, 외부 쿼리에도 날짜 WHERE 필터링을 걸어야 한다.
4. 0인 경우에 출력하지 않는다.
   - 굳이 구현할 필요없이 GROUP BY할 때 자동으로 제거해준다.
