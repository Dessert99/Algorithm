[https://school.programmers.co.kr/learn/courses/30/lessons/276034?language=mysql](https://school.programmers.co.kr/learn/courses/30/lessons/276034?language=mysql)
```sql
SELECT ID, EMAIL, FIRST_NAME, LAST_NAME

FROM DEVELOPERS

-- 두 값의 비트 연산 결과가 0 이상이라면 스킬을 보유하고 있다.
WHERE SKILL_CODE & (
    SELECT SUM(CODE)  -- 모든 컬럼의 CODE 값을 집계
    
    FROM SKILLCODES
    
    WHERE NAME IN ("Python", "C#") -- NAME 파이썬과 C# 컬럼
) > 0

ORDER BY ID


/*
💡 두 테이블은 연관되어 있지 않다.
    -> 서브 쿼리를 사용해야 한다.
*/
```
### 🔗 풀이
1. 조회 (SELCET)
    - ID
    - 이메일
    - 이름
    - 성
2. 어디서 (FROM)
    - DEVELOPERS 테이블
3. 조건 (WHERE)
    - Python이나 C# 스킬을 가진 개발자
    - 비트 연산자 활용
        - `SKILL_CODE` 와 `CODE` 를 비트연산한다.
    - 서브 쿼리
        - 두 테이블이 연관이 없으니 서브 쿼리를 사용한다.
          - IN 연산자
              - 깔끔한 코드를 위해 IN 연산자를 사용한다.
4. 정렬 (ORDER BY)
    - ID를 기준으로 오름차순 정렬

### 🔗 배운점
1. SQL 비트연산자
    - 비트 AND (`&`)
        
        > 두 비트가 **모두 1**일 때만 1을 반환
        > 
        - 활용: 특정 권한(Flag)이 켜져 있는지 확인할 때.
            
            ```sql
              0101 (5: 읽기+쓰기)
            & 0100 (4: 쓰기)
            -------
              0100 (4) -> 0이 아니므로 "쓰기 권한 있음!"
            ```
            
    - 비트 OR (`|`)
        
        > 둘 중 **하나라도 1**이면 1을 반환
        > 
        - 활용: 권한을 추가할 때.
            
            ```sql
              0001 (1: 읽기)
            | 0100 (4: 쓰기)
            -------
              0101 (5: 읽기+쓰기 상태로 변신!)
            ```
            
    - 비트 XOR (`^`)
        
        > 두 비트가 **다를 때만** 1을 반환
        > 
        - 활용: 스위치를 껐다 켰다(Toggle) 하거나, 두 데이터의 차이를 찾을 때.
            
            ```sql
            1(01) ^ 3(11) = 2(10)
            ```
            
2. 이 문제에서 비트 연산자 사용하기
    
    > `&` 연산은 "겹치는 비트들의 값(숫자) 그 자체"를 반환한다.
    만약 1024번째 자리에서 겹쳤다면, 1이 아니라 1024를 뱉어낸다.
    > 
    
    ```sql
    자리수(값):  [256] [128] [64] [32] [16] [8] [4] [2] [1]
    -------------------------------------------------------
    내 스킬  :    1     1    0    0    1    0   0   0   0   (400)
    찾는 거  :    1     0    0    0    0    0   0   0   0   (256)
    -------------------------------------------------------
    & 연산   :    1     0    0    0    0    0   0   0   0
                 (↑)
              (여기만 둘다 1이니까 살아남음!)
    ```
    
    - 겹치는 자리가 없으면 0을 반환한다. 그래서 `SKILL_CODE` 와 `CODE` 를 비트 연산(`&`)했을 때, 0이 반환되면 해당 스킬을 보유하고 있지 않은 것이다.
    - 참고로, 겹치는 자리가 여러개라면 모든 숫자가 합쳐져서 나온다.
        
        ```sql
        자리수(값):  [8]   [4]   [2]   [1]
        ----------------------------------
        필터(10):     1     0     1     0
        내 스킬(15):  1     1     1     1
        ----------------------------------
        & 결과 :      1     0     1     0
                     (↑)         (↑)
                  (여기랑      여기가 겹침!)
                  
        -> 10 출력
        ```
        
3. `IN` 연산자
    
    > 여러 개의 `OR` 조건을 한 방에 처리해준다.
    > 
    
    ```sql
    ❌
    WHERE NAME = 'Python' 
       OR NAME = 'C#' 
       OR NAME = 'Java' 
       OR NAME = 'Go'
       
    ------
    ✅
    WHERE NAME IN ('Python', 'C#', 'Java', 'Go')
    ```
