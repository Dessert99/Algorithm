[https://school.programmers.co.kr/learn/courses/30/lessons/131530?language=mysql](https://school.programmers.co.kr/learn/courses/30/lessons/131530?language=mysql)
```sql
WITH NEW_TABLE AS (
    SELECT
        PRODUCT_ID,
        (
        -- 만원 단위 아래(천원, 백원 등)를 모두 0으로 버린다.
         TRUNCATE(PRICE, -4)
        ) AS PRICE_GROUP
    FROM
        PRODUCT
)


SELECT
    PRICE_GROUP,
    COUNT(PRICE_GROUP) AS PRODUCTS

FROM
    NEW_TABLE

GROUP BY
    PRICE_GROUP
    
ORDER BY
    PRICE_GROUP
```
### 🔗 풀이
1. 🤔 만원 단위의 가격대 별로 상품 개수를 출력
     - 가격대 컬럼을 새로 만든다.

### 🔗 배운점
1. `TRUNCATE(숫자, 자릿수)`
   - 숫자: 자르고 싶은 원래 숫자 (또는 컬럼명)
   - 자릿수: 어디까지 남길 것인가를 결정하는 핵심 포인트! (양수, 0, 음수에 따라 작동이 확 달라진다.)
2. 자릿수(N)에 따른 작동 방식 완벽 정리
   - 기준: 12345.678
     | **자릿수 설정** | **수식** | **결과** | **설명** |
      | --- | --- | --- | --- |
      | **양수 (+)** | `TRUNCATE(12345.678, 2)` | `12345.67` | 소수점 아래 **2번째 자리까지 남기고** 뒤는 버림 |
      | **0** | `TRUNCATE(12345.678, 0)` | `12345` | 소수점 아래를 싹 다 버리고 **정수**로 만듦 |
      | **음수 (-)** | `TRUNCATE(12345.678, -1)` | `12340` | 소수점 위 **일의 자리(1개)를 0으로** 만듦 (10 단위) |
      | **음수 (-)** | `TRUNCATE(12345.678, -2)` | `12300` | 소수점 위 **십의 자리까지(2개) 0으로** 만듦 (100 단위) |
      | **음수 (-)** | `TRUNCATE(12345.678, -3)` | `12000` | 소수점 위 **백의 자리까지(3개) 0으로** 만듦 (1,000 단위) |
      | **음수 (-)** | `TRUNCATE(12345.678, -4)` | `10000` | 소수점 위 **천의 자리까지(4개) 0으로** 만듦 (10,000 단위) |
     - 꿀팁
       - 음수를 쓸 때는 "0을 몇 개 만들 건가?"라고 생각하면 편하다.
        -4를 넣으면 끝자리가 0000(0이 4개)이 되니까 '만 원 단위'가 된다.
3. 응용 패턴
   - `TRUNCATE() + GROUP BY`
     - ① 나이(AGE)를 연령대(10대, 20대...)로 묶을 때
       ```sql
        -- 27살 -> 20, 34살 -> 30으로 변환됨
        SELECT TRUNCATE(AGE, -1) AS 연령대, COUNT(*) AS 인원수
        FROM USERS
        GROUP BY TRUNCATE(AGE, -1);
       ```
     - ② 가격(PRICE)을 만원 단위로 묶을 때
       ```sql
        -- 15400원 -> 10000, 29900원 -> 20000으로 변환됨
        SELECT TRUNCATE(PRICE, -4) AS 가격대, COUNT(*) AS 상품수
        FROM PRODUCT
        GROUP BY TRUNCATE(PRICE, -4);
       ```
