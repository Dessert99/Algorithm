[https://school.programmers.co.kr/learn/courses/30/lessons/42860?language=javascript](https://school.programmers.co.kr/learn/courses/30/lessons/42860?language=javascript)
```javascript
function solution(name) {
    let answer = 0;
    const n = name.length;

    // name을 아스키 코드로 변환한 배열
    const nameToAsc = [];
    for (let i = 0; i < n; i++) {
        nameToAsc.push(name.charCodeAt(i));
    }

    // 상-하 움직이는 횟수
    nameToAsc.forEach((target) => {
        // A와 가까운가
        const fromA = target - 65;

        // Z와 가까운가
        const fromZ = 91 - target; // A->Z로 이동하는 거리 1이 포함되어 있다.

        answer += Math.min(fromA, fromZ);
    });

    // 좌-우 움직이는 횟수

    // 기본값: 오른쪽으로만 갔을 때
    let minMove = n - 1;
    for (let i = 0; i < n; i++) {
        // i와 next는 "다시 돌아갈 기준점이다."
        let next = i + 1;

        // 65라면 건너뛴다.
        while (next < n && nameToAsc[next] === 65) {
            next++;
        }

        // 시작점 -> i -> 시작점 복귀 -> 왼쪽으로 next까지 길이
        const rightFirst = i + i + (n - next);

        // 시작점 -> 왼쪽으로 next까지 길이 -> 시작점 복귀 -> i 까지 길이
        const leftFirst = n - next + n - next + i;

        //갱신
        minMove = Math.min(minMove, rightFirst, leftFirst);
    }

    return answer + minMove;
}
```
### 🔗 문제풀이
1. name을 아스키 코드로 변환한다. (계산하기 쉽게)

>⭐️ 상-하/좌-우 횟수를 분리하여 계산하는 것이 가독성이 좋다고 판단했다.

1. 상-하 움직이는 횟수 구하기
    1. name 순회한다.
    2. 매번 A와 가까운지, Z와 가까운지 판단한다.
    3. 가까운쪽을 카운팅한다.
2. 좌-우 움직이는 횟수 구하기
    
    >⭐️ 단방향이 아닐 수도 있어서, 매번 “지금 다시 돌아가는게 효율적인가?” 를 판단한다. 즉, 시작부터 이 위치까지 갔다가, 다시 돌아가서 다음 위치까지 가는 최소 거리를 파악한다.
    
    1. minMove 기본값을 설정한다. 오른쪽으로 직진하는 케이스이다.
    2. name를 순회하며 모든 위치에서 판단한다.
    3. A를 제외하고 얼마나 이동해야 하는지 next를 계산한다.
    4. 3가지 케이스를 비교하여 최소 거리를 갱신한다.
        - 기존 minMove
        - 시작점 -> i -> 시작점 복귀 -> 왼쪽으로 next까지 길이
        - 시작점 -> 왼쪽으로 next까지 길이 -> 시작점 복귀 -> i 까지 길이
### 🔗 배운점
1. 아스키 코드 변환 메서드
   ```javascript
      const str = 'ABC';
      // 1. 첫 번째 문자 'A'의 아스키코드
      console.log(str.charCodeAt(0)); // 출력: 65
      
      // 2. 두 번째 문자 'B'의 아스키코드
      console.log(str.charCodeAt(1)); // 출력: 66
      
      // 3. 소문자 'a'의 아스키코드
      console.log('a'.charCodeAt(0)); // 출력: 97
      
      // 4. 공백의 아스키코드
      console.log(' '.charCodeAt(0)); // 출력: 32
   ```
   ```javascript
    // 1. 숫자 65를 문자로 변환
    console.log(String.fromCharCode(65)); // 출력: "A"
    
    // 2. 숫자 97을 문자로 변환
    console.log(String.fromCharCode(97)); // 출력: "a"
    
    // 3. 여러 숫자를 한 번에 문자로 변환하여 합치기
    console.log(String.fromCharCode(72, 69, 76, 76, 79)); // 출력: "HELLO"
   ```
