[https://school.programmers.co.kr/learn/courses/30/lessons/86491?language=javascript](https://school.programmers.co.kr/learn/courses/30/lessons/86491?language=javascript)
```javascript
function solution(sizes) {
    let answer = 0;
    // 긴 쪽이 가로가 되도록 정규화
    const normalizationSizes = sizes.map((card) => {
        const [width, height] = card;
        if (width < height) {
            return [height, width];
        }
        return card;
    });

    // 가로, 세로 각각 가장 큰 값을 구한다.
    let maxWidth = 0;
    let maxHeight = 0;

    normalizationSizes.forEach((card) => {
        const [curWidth, curHeight] = card;

        if (curWidth > maxWidth) {
            maxWidth = curWidth;
        }

        if (curHeight > maxHeight) {
            maxHeight = curHeight;
        }
    });

    answer = maxWidth * maxHeight;

    return answer;
}
```
### 🔗 배운점
1. 데이터가 복잡하다면 정규화시켜버리자.
    - 이번 문제같은 경우는, 가로-세로 중 긴 값을 가로로 정의하고, 짧은 값을 세로로 정의하면 쉽게 비교할 수 있었다. 일일히 `if-esle`문으로 복잡한 비교를 하지 않아도 된다.
    - 언제 유용할까?
        
        > 데이터의 **순서나 방향이 중요하지 않을 때**
        > 
        
        > 조건문(If-else)이 너무 많아져서 모든 경우의 수를 따지기 복잡할 때.
        >
