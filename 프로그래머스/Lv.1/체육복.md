[https://school.programmers.co.kr/learn/courses/30/lessons/42862?language=javascript](https://school.programmers.co.kr/learn/courses/30/lessons/42862?language=javascript)
```javascript
function solution(n, lost, reserve) {
    // 전체 학생: 2 <= n <=30
    let answer = 0;

    // 여벌 체육복을 가져온 학생이 체육복을 도난당한 경우를 제거해야 한다.
    const realReserve = reserve.filter((i) => !lost.includes(i)).sort((a, b) => a - b);
    const realLost = lost.filter((i) => !reserve.includes(i)).sort((a, b) => a - b);

    // 일단 여벌 체육복을 가져온 학생 카운팅
    answer += n - realLost.length;

    // 잃어버린 학생 조사
    for (let student of realLost) {
        const prevStudent = student - 1;
        const nextStudent = student + 1;

        // indexOf로 앞/뒷번호 여벌 학생이 있는지 조사
        const prevIdx = usableReserves.indexOf(prevStudent);
        const nextIdx = usableReserves.indexOf(nextStudent);

        // 있다면 splice로 realReserve 배열 수정
        if (prevIdx !== -1) {
            realReserve.splice(prevIdx, 1);
            answer++;
        } else {
            if (nextIdx !== -1) {
                realReserve.splice(nextIdx, 1);
                answer++;
            }
        }
    }
    return answer;
}
}
```
### 🔗 배운점
1. 시간복잡도는 꼭 계산하자.
   - 이 문제에서 괜히 매번 include로 확인하면 시간복잡도를 초과할까 걱정했는데, 사실 `n`이 최대 30이다. 계산해보면 넉넉하다는 사실을 알 수 있다.
2. `filter()`는 원본 배열을 바꾸지 않는다..
   ```javascript
    realReserve.filter((item) => item !== L - 1); // 제거
   ```
   - 이 코드는 체육복을 빌려준 뒤, 목록에서 제거할 수 없다. `filter()`는 원본 배열을 바꾸지 않기 떄문이다. 여전히 `realReserve`배열에는 체육복을 빌려준 친구가 남아있다.
3. `indexOf()`, `splice()`사용법
   ```javascript
      const friends = ['철수', '영희', '민수'];
      console.log(friends.indexOf('영희')); // 1 (1번 방에 있음)
      console.log(friends.indexOf('바둑이')); // -1 (없음)
   ```
   ```javascript
    const numbers = [10, 20, 30, 40, 50];
      // 2번 인덱스(30)부터 1개를 지워라!
      numbers.splice(2, 1); 
      console.log(numbers); 
      // 결과: [10, 20, 40, 50] -> 30이 사라짐 (원본이 바뀜!)
   ```
4. 순회를 앞에서부터 돌면, 앞번호부터 확인하는게 맞다.
   - 뒷번호부터 확인하면 선택지가 좁아져서 확인하지 못하는 번호가 생긴다.
